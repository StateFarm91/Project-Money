"""Visual commerce: the canonical model, and the QA that keeps a gallery honest.

Nothing here pretends a canonical identity has been selected. The drift evaluator reports
`unavailable` rather than `pass`, because a check that passed for want of a reference would be
believed.
"""
