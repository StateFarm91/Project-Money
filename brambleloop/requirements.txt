sqlalchemy>=2.0
fastapi>=0.115
uvicorn[standard]>=0.30
jinja2>=3.1
reportlab>=4.0
pillow>=10.0
psycopg2-binary>=2.9
requests>=2.31
cryptography>=42
python-multipart>=0.0.9
pypdf>=4.0
# Yarn-level geometry (visual/yarn.py). Small, standard, and the module is not importable
# without it. Mitsuba is deliberately NOT here: the physically based renderer is ~200MB and
# is only needed to run the feasibility spike by hand, never by the deployed service, so the
# production image should not carry it.
numpy>=2.0
