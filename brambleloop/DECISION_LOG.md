# DECISION_LOG — Brambleloop Studio

Consequential decisions with reasoning, so future sessions do not re-litigate them.

| # | Date | Decision | Why | Reversible? |
|---|---|---|---|---|
| B-001 | 2026-09-17 | Project-Money 90-day competition retired; Brambleloop Studio is the active mission. | Owner directive: "Scrap the competition. Competition is cancelled." Handoff package v1.2 supplied as canonical spec. | Owner's call |
| B-002 | 2026-09-17 | Python 3.11 for the core, not Node/TS. | The differentiating work is deterministic arithmetic, PDF/chart generation and image compositing. Python's reportlab/Pillow are already proven working in this environment. A JS frontend is not needed for an admin dashboard. | Yes, but costly |
| B-003 | 2026-09-17 | Postgres-backed durable queue (`FOR UPDATE SKIP LOCKED`), not Redis/Celery/SQS. | Master Plan section 13 demands durable jobs with leases, retries and idempotency. Postgres already has to exist for the warehouse; adding a second datastore adds a component that can die unattended at 3am with nobody watching. Fewer moving parts is a reliability decision, not a laziness one. | Yes |
| B-004 | 2026-09-17 | SQLite for tests, Postgres for production, behind one abstraction. | Acceptance tests must run hermetically anywhere, including before any cloud account exists. SKIP LOCKED degrades to a transactional claim on SQLite. | Yes |
| B-005 | 2026-09-17 | Writer and reverse compiler share **no** parsing code. | If they shared a grammar, a round-trip test would prove only that the bug is symmetric. Master Plan section 3 requires the reverse compiler to see "only the customer pattern". Independence is the whole point. | No — this is load-bearing |
| B-006 | 2026-09-17 | Digital twin refuses to model a pattern that failed compilation. | A twin built on broken arithmetic is fiction, and every downstream gate (Asset Truth, size claims, yardage) would then be reasoning about an object that cannot exist. | No |
| B-007 | 2026-09-17 | Yardage is an estimate with an explicit ±20% tolerance and a `calibrated` flag. | Yarn consumption genuinely varies by yarn, hook and tension. Inventing a precise number would be the exact "unsupported claim" the Policy Gate exists to block. Physical tests calibrate it (Master Plan section 3). | Yes, as data arrives |
| B-008 | 2026-09-17 | Magic ring modelled as a `foundation_kind`, not a free-text note. | The writer was emitting "sc in next 6 sts" for a magic-ring round — nonsense to a maker. An override string would have created a hole in reverse-compilation verification; a structural field keeps the check intact. | Yes |
| B-009 | 2026-09-17 | Built under `brambleloop/` in the Project-Money repo rather than a new repo. | Push credentials are scoped to `StateFarm91/Project-Money`. Blocking real progress on repo cosmetics would violate the directive's "do not stop because one integration is unavailable; route around it". Migration to a dedicated repo is trivial later and is tracked in BUILD_STATE. | Yes |
| B-010 | 2026-09-17 | Cloud not provisioned yet despite a verified Railway account. | Master Plan section 35 defers paid infrastructure approval until the system reaches the integration that needs it. Provisioning an empty project now would spend the owner's money to host nothing. Deploy when there is a service worth running. | Yes |
| B-011 | 2026-09-17 | Opportunity scoring is a weighted sum of six named components, computed in code. No model ranks concepts. | A model is good at proposing concepts and unreliable at being consistent about why one beats another. "The model preferred it" is not a reason the owner can overrule six weeks later when a SKU underperforms. Every score decomposes into demand, competition headroom, verifiability, seasonal fit, margin and whitespace, and the weights are one editable dict. | Yes — weights are data |
| B-012 | 2026-09-17 | Seasonal fit is evaluated at **our ready date**, using **the concept's own** maker hours, not the event date and not generic ones. | Two separate errors this avoids. Ranking by days-until-the-holiday would schedule Christmas blankets for December, when no buyer can finish one. Using the event's generic make time would give a stocking and a throw the same deadline, and they are six weeks apart. Scoring at today's date rather than the date we could actually list is a third: a concept we cannot ship for three weeks must be judged on the market that will exist in three weeks. | Yes |
| B-013 | 2026-09-17 | Portfolio selection applies structural constraints and will deliberately take a lower-scoring concept to meet them. | Section 33 forbids a first portfolio that depends on a single demand pattern. Pure ranking produced four Christmas items and no fast movers. Every swap is recorded with both scores and the constraint it served, so the trade is visible rather than buried. | Yes |
| B-014 | 2026-09-17 | Bundles are derived inventory: excluded from the ranked competition, appended only when their family earned at least two places. | The bundle scores highest in the pool because it is cheap and high-margin — but only once its members exist. Ranking it as an independent SKU produced a portfolio containing a bundle of products we had not built, which is a listing we cannot fulfil. | Yes |
| B-015 | 2026-09-17 | Competitor profiles carry an observation date and a URL, and are asserted on in tests. | Market evidence rots silently. An undated price band read as current a year later is worse than no data, because it looks like data. | No |
| B-016 | 2026-09-17 | Stitch taxonomy carries `height` (turning-chain units) and `row_height` (physical) as separate fields. | They were one field. A dc turns with three chains and is about twice the height of a sc; conflating them overstated a blanket's finished length by half. Finished length is a size claim on a listing, so this was a refund waiting to be printed, not a rounding issue. | No |
| B-017 | 2026-09-17 | Yarn constants are anchored on a checkable reference, and the anchor is written down next to them. | The first table was set by feel and came out at less than half of a defensible figure — a throw would have been advertised as needing 190 m. An estimate is allowed to be wrong by its stated tolerance; being wrong by a factor of two is not an estimate. Still uncalibrated at +/-20% until physical tests replace it. | Yes, on evidence |
| B-018 | 2026-09-17 | The flagship's pattern is generated from a motif grid, and each row is emitted as a repeat rather than flattened. | Flattening twelve repeats of a twelve-stitch motif produced a row instruction eleven lines long: physically unusable, and the kind of thing that makes a maker lose their place and blame the pattern. The repeat form is both what a human follows and what the compiler checks for divisibility, so readable and verifiable are the same form. | Yes |
| B-019 | 2026-09-17 | Large charts are published as one detected repeat plus a placement note, never as the whole piece. | A 144 x 120 chart on one page gives each stitch about a pixel. The repeat is *computed* from the fabric rather than asserted in copy, so the chart cannot claim a repeat the pattern does not have. | Yes |
| B-020 | 2026-09-17 | Web, worker and scheduler run in one container for now. | Railway bills by resources consumed and the company has no revenue; three always-on services cost roughly three times one. Correctness does not depend on the split — the queue is durable with leases and idempotency keys — so splitting later is a start command, and `worker_entry.py` and `scheduler_entry.py` already exist for that day. | Yes |
| B-021 | 2026-09-17 | With `BRAMBLELOOP_REQUIRE_POSTGRES=1` the app refuses to start rather than fall back to SQLite. | A container that silently falls back to a file on ephemeral disk passes every health check, renders the dashboard, and destroys every job, audit record and release certificate on its next restart. Failing to start is enormously preferable to a company whose memory quietly resets. | No |
| B-022 | 2026-09-17 | Artifact bytes are stored on local disk; artifact hashes are stored in Postgres. | No object storage is provisioned. The split means the system can always prove the PDF it is about to ship is the one that was certified, and when a file is gone it knows it is gone instead of serving a different one. Every run audits `assets.storage_not_durable` rather than implying durability it does not have. | Yes, swap in S3 |
| B-023 | 2026-09-17 | Prompts are immutable, content-hashed and referenced as `name@version`. | A prompt is a dependency, not a string. If it can be edited in place then two runs of the same job are not comparable, a regression cannot be attributed, and the audit trail records a call to something that no longer exists. Editing means publishing a new version; the old text stays so last month's output can still be explained. | No |
| B-024 | 2026-09-17 | No registered prompt may ask a model for pattern content, and this is enforced by a test. | Section 2 is absolute and a standing constraint is worth nothing if it lives only in a docstring. The prompt set is deliberately narrow: naming, readability polish that may not change a fact, and review mining. Everything load-bearing stays deterministic. | No |
| B-025 | 2026-09-17 | `ModelGateway.require_deterministic()` raises rather than returning a model verdict. | Section 27 says deterministic validation wins even if every LLM disagrees. Making it a method rather than a comment means a future caller reaching for a model opinion on pattern correctness gets an exception instead of a code review that may not happen. | No |
| B-026 | 2026-09-17 | The gateway ships with a deterministic `EchoProvider` and reports zero real providers. | No API key exists in this environment. Echo mocks the transport — pinning, parsing, cost, failover, circuit breaking — never judgement, which eval fixtures test instead. `available_providers()` returning an empty list is the truthful answer, and the dashboard shows it. | Yes, when a key exists |
| B-027 | 2026-09-17 | Agent permissions and ceilings are declarative: `seed_defaults` reconciles them on every boot. | Insert-if-absent meant a permission fixed in code never reached a database that already had the agent. Production proved it: the heartbeat fix deployed and the heartbeat kept failing. A permission that lives in code and cannot reach the database is not a permission, it is a comment. Runtime state such as `enabled` is left alone. | No |
| B-028 | 2026-09-17 | The container becomes healthy before it takes on load (`BRAMBLELOOP_RUNNER_START_DELAY`). | Two deployments were marked failed by the platform while working perfectly: the embedded worker began a full planning cycle at startup, and rendering eleven products' PDFs is CPU-bound work holding the GIL, which starved the health endpoint past the timeout. Serving first and working second is how a container is supposed to start. | Yes |
| B-029 | 2026-09-17 | Dead letters can be re-driven, but a closed capability gate can never be. | A dead letter from a fixed defect is work the company still owes, and leaving it red for a day teaches an operator to ignore the signal. Re-driving a shadow-mode publish refusal, by contrast, is an operator repeatedly asking a closed gate to open. A permission denial IS re-drivable: it reads like a refusal and is usually a misconfiguration, and the worker re-authorises on dispatch so a still-missing permission simply fails again. | Yes |
| B-030 | 2026-09-17 | `/api/verify` evaluates the standing safety assertions against live state and returns 503 if any fails. | "Railway says deployed" is not "the company is alive and behaving". The questions an absent owner actually has — is it running, is its memory durable, has anything escaped shadow mode, are the money guards on — are now answerable in one request, with the evidence for each check attached so a green result can be argued with. | No |
| B-031 | 2026-09-17 | The brand is data — palette, type, crop rules, naming grammar, character bible — not taste. | Consistency maintained by judgement alone does not survive a system producing assets unattended for months. `check_grid_coherence` can say whether the storefront still reads as one shop without anyone looking at it. | Yes |
| B-032 | 2026-09-17 | The brand model is refused for product-first categories, refused any real-person likeness, and can never be the hero. | Section 6 says product-first imagery is stronger for blankets and decor, and a model holding a blanket hides the pattern being sold. A likeness of a real person is refused however the request is phrased. An AI lifestyle concept as hero would be a picture of a thing nobody has made presented as evidence the thing exists. | No |
| B-033 | 2026-09-17 | Listing imagery is part of the release: rendered from the twin, class-declared, Asset Truth-checked, and blocking. | Imagery assembled after certification is imagery nothing verified. Rendering every frame from the same twin as the PDF means no frame can assert something the pattern does not produce, and a listing whose imagery misrepresents the pattern now stops the chain before pricing. | No |
| B-034 | 2026-09-17 | Search coverage is scored against *reachable* queries, not the biggest ones. | A shop with no history is not placing for "crochet blanket pattern", and scoring against it would tell the system to spend all thirteen tag slots on queries it will never appear for. Technique tags are gated on techniques the pattern actually uses: ranking for something we are not is a bounce, not a win. | Yes |
| B-035 | 2026-09-17 | Pricing optimises expected contribution per visitor, never gross revenue or lowest price. | A cheaper pattern converts better and can still earn less per visitor; a dearer one can earn more in total while killing the review accumulation a new shop depends on. Every figure is net of Etsy's real fee stack, because a decision made on gross is made on a number that never reaches the bank. | Yes |
| B-036 | 2026-09-17 | A price experiment's stopping rule is fixed before it starts, and a sub-8% price difference is refused at design time. | A test whose stopping rule is decided while looking at the results is a way of confirming what the operator already believed. At the sample sizes a new shop can reach, a 3% price difference is unmeasurable. | No |
| B-037 | 2026-09-17 | Thumbnail composition is checked now; hero ranking waits for real impressions and refuses without them. | Section 7 asks for both. The compositional half works today because a hero can be downscaled to Etsy's real grid size and measured. The evidence half cannot: inventing plausible CTRs to make a ranking function return something is the fabricated engagement section 8 forbids, applied to ourselves. | Yes, when data exists |
| B-038 | 2026-09-17 | Support triage puts escalation first in the routing table, and every reply is recorded as unsent. | A message containing both "chargeback" and "yarn" is a dispute; ordering is what stops a legal matter reaching the materials desk. And "we could not have sent it" is a weaker guarantee than "we recorded that we did not". | No |
| B-039 | 2026-09-17 | The portfolio classifier returns NO_EVIDENCE rather than a diagnosis for a SKU nobody has seen. | Every SKU has zero impressions. A classifier that read that as CONVERSION_PROBLEM would retire a catalogue that has simply never been shown to anyone — the most expensive mistake available to this component. | No |
| B-040 | 2026-09-17 | Confidence is tracked as seven separate dimensions with deliberately no overall score. | An average is more confident than the guess and less confident than the proof. Arithmetic confidence is near-total and yardage confidence is 0.45; averaging them would let a listing imply the yarn figure is as solid as the stitch counts. Physical confidence starts at zero and no amount of computation moves it. | No |
| B-041 | 2026-09-17 | Regression fixtures store the defective pattern, not the fix, and a clean pattern cannot become one. | A suite made of fixed patterns proves only that the fixed patterns are still fixed. A fixture that provokes no finding asserts nothing, and a suite full of those reports green while testing nothing. | No |
| B-042 | 2026-09-17 | Products are generated from a validated motif library; a width the motif cannot tile is refused. | Engineering twelve products by hand is twelve chances to slip. A truncated motif compiles perfectly and leaves half a fir tree down one side — a defect no arithmetic will ever catch, so the builder refuses rather than fudges. | Yes |
| B-043 | 2026-09-17 | A bundle has no pattern of its own and is assembled from its members' certified releases. | The pipeline had been certifying a "Nordic Forest Collection Bundle" whose PDF was a twelve-row striped panel: a product sold as three patterns and delivered as one invented swatch. A collection now lists only members that actually shipped, and waits when fewer than two have. | No |
| B-044 | 2026-09-17 | The books are built while every figure is zero, and the tax reserve is never counted as ours. | Accounting that first appears alongside the first sale is accounting nobody has checked; accounting built while the answer is obviously zero can be checked against an answer everyone knows. GST/HST is money held for a government, and counting it as revenue is how a small business discovers a liability it has already spent. | No |
| B-045 | 2026-09-17 | The CA$100K trajectory refuses to produce a run rate or a projection from zero orders. | It is the number most likely to be quoted back as though it were a forecast. With no orders there is no run rate; what the function returns instead is arithmetic on the target, labelled as arithmetic. | No |
| B-046 | 2026-09-17 | The CFO/Skeptic reports concerns even when nothing is wrong. | A reviewer who only speaks up during a crisis is a reviewer nobody has calibrated. It blocks on infrastructure above the owner's ceiling and on advertising with no attributable orders, and notes the burn and the tax reserve every time. | Yes |
| B-047 | 2026-09-17 | Paid-media guard rails are built now, while no campaign exists and no money can be spent. | Advertising is the one part of this system that can lose money quickly and silently. Guard rails written after the first campaign are guard rails written by someone who already knows what they cost. `authorise_spend` refuses on phase and on missing owner authority before it looks at any budget: a cap is not permission. | No |
| B-048 | 2026-09-17 | Broken tracking pauses a campaign before any budget question is asked. | A campaign running with broken attribution is not one that might be losing money; it is one nobody can tell is losing money, and a budget cap does not protect against being blind. | No |
| B-049 | 2026-09-17 | Every acceptance-gate line has its own named test. | "Gate B passes" was previously inferred from a scattering of component tests. Now each line of the owner's acceptance document has a test that names it, so a gate claim is checkable, and a second Gate F test asserts that nothing actually graduated. | No |
| B-050 | 2026-09-17 | Difficulty claims are checked in both directions. | The rule only caught "beginner" on a Class C pattern, which left the gate line "unsupported difficulty claims are blocked" half true. Overstating difficulty is just as unsupported as understating it, and it turns away the buyer the product was designed for. | Yes |
| B-051 | 2026-09-17 | Every idempotency key after certification carries a chain version. | Production proved the need: fifteen products were certified before listing imagery, search coverage and the content ecosystem existed, and the next cycle collapsed every downstream job against keys that were already taken — reporting success and producing nothing. Certification is correctly once per version; everything after it is different work when the code that does it changes. | Yes |
| B-052 | 2026-09-17 | `chain.rebuild` runs hourly and restarts certified releases that have no current listing. | Bumping the chain version makes the keys free; something still has to notice. A certified product with no listing is a product that cannot be sold, which is worth checking for on a cadence rather than only after a deploy. | Yes |
| B-053 | 2026-09-17 | A listing-copy change bumps the chain version so it reaches products already shipped. | The first title generator produced "Pet Snuggle Mat \| ... \| Pet Pet \| ..." and it went to production. A stuttering title is the clearest possible signal that nobody read the listing before it went up, and fixing the generator without bumping the chain would have left the stutter on every shipped product. | Yes |
| B-054 | 2026-09-17 | `create_all` is followed by an additive schema migration that adds missing columns and their indexes. | `create_all` does not touch a table that already exists, so a column added in code never reaches a deployed database. This is the third instance of one problem — code changed, the deployed database did not — after the agent permission and the release chain. Additive only: it will not drop, narrow, rename or backfill, and it refuses a NOT NULL column with no default rather than guessing. | No |
| B-055 | 2026-09-17 | Listings record the chain version that produced them, and the rebuild restarts stale ones, not only missing ones. | The first rebuild asked whether a listing *existed*. A stale listing exists, so a copy fix reached nothing and every shipped product kept a stuttering title through two deploys. Staleness has to be visible to be actionable. | No |
| B-056 | 2026-09-17 | An added column is backfilled with the model's literal default. | Without it a fresh database has the column NOT NULL with a value and a migrated one has it NULL, so the same code reads two different shapes — and the comparison deciding whether a row is stale silently compares against None. Only literal defaults are backfilled; a callable default is per-row logic this module has no business guessing. | No |
| B-057 | 2026-09-17 | Row-level repeats are derived from the compiled rows, never declared in the CIR. | A declared repeat is a second statement of the same fact, and two statements can disagree while both stay internally consistent — no arithmetic would catch it, because both halves add up. `detect_cycle` computes the cycle from the rows the compiler already validated, so there is no field to get wrong and nothing to keep in sync. The cost is that an intended repeat the designer wants stated but the rows do not support cannot be expressed; that is the right refusal. | No |
| B-058 | 2026-09-17 | The reverse compiler parses and expands the repeat instruction itself, from the customer text alone. | The obvious shortcut is for the writer to hand the reader its expansion. Then the two are no longer independent and the whole validation chain checks its own work (B-005). So the reader has its own regex and its own expansion, and it refuses a repeat that points at rows the text never printed. A reader that ignored the instruction instead would report every collapsed pattern as ninety rows short, which is worse than not supporting repeats at all. | No |
| B-059 | 2026-09-17 | A piece worked in the round is measured as a surface of revolution, not as flat fabric. | The flat model reads stitches-around as a width. Thirty stitches at 20 sts/10cm is 15 cm of *circumference*, which is a piece under 5 cm across, so a listing built on it advertises a three-inch bauble as a six-inch one. One relationship covers every case -- row height squared equals radius change squared plus axial rise squared -- and it also explains why "+6 sc per round" makes a flat circle, and tells us when the fabric must gather. | Yes |
| B-060 | 2026-09-17 | The twin refuses a width and a height for a closed shaped piece, and states a circumference instead. | Rounds 2-5 of every amigurumi grow at almost exactly one row height, which is geometrically the flat case, so the surface model sincerely reports a hemisphere as a half-centimetre pancake. It is not wrong about the fabric; the ball is made by the stuffing and the maker's tension, and neither is in the gauge. A circumference is arithmetic and a height is a physical measurement, so we give the first and refuse the second (section 3). | No |
| B-061 | 2026-09-17 | A product's own name is checked against the shape the pattern makes. | "Market Basket Trio" was a single flat rectangle whose designer note admitted it was "the side panel, seamed into the basket", with no seaming instruction in the document; "Hexagon Coaster Set" was a rectangle. Both were certified and drafted into listings. The check is deliberately narrow -- a three-dimensional noun, or a shape word attached to the object -- because mosaic work legitimately depicts stars and hearts *on* rectangles and over-flagging that would block real products. | Yes |
| B-062 | 2026-09-17 | A size claim the twin cannot check is refused rather than ignored. | The claim check skipped any dimension where the twin had no number, which is exactly the case where a claim is least supportable: the piece the twin refuses to measure. "Nothing to compare it against" was being treated as "nothing to object to". | Yes |
| B-063 | 2026-09-17 | Written instructions name the yarn on every row of a multi-colour pattern, and the reverse compiler checks it. | The whole catalogue's written instructions omitted colour: a two-colour mosaic was only workable from the chart. In overlay mosaic the colour *is* the motif, so a document that names the wrong yarn produces a different object, which makes colour a structural check rather than a presentational one. | Yes |
| B-064 | 2026-09-17 | How the rounds are worked is stated once per component and read back from the text. | Joining each round leaves a seam up the side; spiralling does not. A round-worked document that says neither is missing an instruction the maker needs, and one that says the opposite describes a different fabric. Stated once rather than repeated on sixty lines, so there is one source of truth to check. | No |
| B-065 | 2026-09-17 | The finishing is part of the CIR, and a flat panel may be called a basket only once something joins it. | Multi-piece patterns without assembly steps are a bag of rectangles and a wish. Seams name their pieces, the compiler checks those pieces exist, the writer emits the steps and the reverse compiler reads them back -- so the shape claim gate can ask a real question: is there anything here that turns these pieces into that object? | No |
| B-066 | 2026-09-17 | The customer document carries its own version, and re-certification replaces the stored certificate. | The release hash is taken over the CIR *and* the written text, so a change to the writer invalidates every certificate issued before it -- while the PDF quietly regenerates from the new writer. Persisting the release was insert-if-absent, which is the fourth appearance of "code changed, the deployed database did not" (B-027, B-051, B-054). | Yes |
| B-067 | 2026-09-17 | A product whose certificate is refused has its listing withdrawn. | Refusing a certificate and leaving the storefront untouched leaves a draft listing for a product the release chain has just rejected. The only thing between that draft and a customer was shadow mode, and shadow mode is a phase, not a guarantee. | No |
| B-068 | 2026-09-17 | Launch readiness separates blockers into build, integration and owner, and only the owner's reach the owner queue. | The directive forbids asking for an Etsy account, KYC or banking "because they will eventually be needed", and the honest test of that is whether the system can tell the difference between a requirement it has not met yet and one it cannot meet at all. Every unmet requirement names who it waits on; a requirement blocked on build never carries an owner request, and the test suite asserts both directions. | No |
| B-069 | 2026-09-17 | The owner queue is written by the system on a daily cadence, not by hand. | The table has had the directive's five columns -- action, reason, maximum cost, minutes, consequence of delay -- since the first build and nothing had ever written to them, because until the catalogue, imagery, pricing and copy existed every blocker was ours. Now that the remaining blockers really are the owner's, the company says so itself, and re-running the assessment does not duplicate what is already queued: an owner queue that grows by seven a day is a queue nobody reads. | No |
| B-070 | 2026-09-17 | Idempotency keys for design-derived work carry the design's own fingerprint, and the rebuild compares the stored design against the one the code produces. | Found by watching production accept the round-worked rebuild and keep serving the old one. `cir.draft` was keyed `draft:{slug}` -- once per slug, forever -- so a re-engineered product could never enter the chain: the draft was done, the compile key was taken, certification never re-ran, and the `DOC_VERSION` bump meant to deliver the fix could not reach the stage it was bumped for. A slug and a version do not identify a design; the design's content hash does. This is the fifth face of "code changed, the deployed database did not", and the one that broke the previous four fixes' own delivery mechanism. | Yes |
| B-071 | 2026-09-17 | The post-certification chain is keyed on the certified release hash, and a listing records which release produced it. | Found in production ten minutes after B-070, which is the same bug one stage lower: certification re-ran, produced a new release hash, and then found `listing:{slug}:{version}:c{chain}` already taken by the previous rebuild, so the corrected design certified and the old listing stayed. The chain version catches a code change; only the release hash catches a design change, because a re-engineered product keeps its slug and its version. | Yes |
| B-072 | 2026-09-17 | A rebuild's trigger is part of the idempotency key and rides the whole chain. | The third layer of one bug, each layer found by watching production refuse to change. A rebuild exists precisely for the case where the work already ran and its result is no longer right, so keying only on the product, the code version and the certified release still leaves every stage's key taken. The rebuild now mints a token from the transition it is correcting and passes it down the chain, so each stage re-runs once for that rebuild and not once per cadence. | Yes |
| B-073 | 2026-09-17 | The rebuild records why it judged each listing current or stale. | It reported "nothing to do" three times while two products sat visibly wrong in production, and from outside the process that claim was unfalsifiable. The audit entry now carries the comparison it made for every listing. | No |
| B-074 | 2026-09-17 | A physical sample calibrates the yarn estimate and falsifies the size claim, and those are kept separate. | Folding a size disagreement into a calibration factor would turn the one check that can catch a wrong geometry model into a number that silently absorbs it. So a yarn difference within a plausible band becomes a factor; a finished size outside 12% of the claim raises a defect against the product instead. The factor is keyed on the yarn and the stitch it was measured on -- a test caught an earlier version keying on weight alone, where "worsted cotton" and "worsted acrylic" collided. | Yes |
| B-075 | 2026-09-17 | The Etsy publishing path is written and unit-tested while it cannot publish. | It is the only code in the system that could reach a customer, and the alternative is writing it for the first time on the day it matters. Three refusals sit in front of every request, in this order: phase, then the owner's authority, then credentials -- so a misconfigured key can never be the reason we stopped, and a present key is never mistaken for permission. The transport is the only way out and the tests count its calls: in shadow, with credentials present, zero requests are made. | No |
| B-076 | 2026-09-17 | The client refuses a listing it cannot map rather than truncating it. | A title cut at Etsy's 140 characters mid-word is a listing nobody approved, published under a certificate that describes something else. The limits are Etsy's and the policy gate already enforces them upstream, so this check should never fire -- which is exactly why it is here. | No |
| B-077 | 2026-09-17 | "Listing created, file not attached" is reported as its own outcome. | A listing on Etsy with no digital file takes money and delivers nothing. It is neither a success nor a plain failure: the recovery is to complete or delete that listing, not to create another one. It also surfaces the dependency on object storage, because a process that renders a PDF, restarts, and then uploads has nothing to send. | No |
| B-078 | 2026-09-18 | A seam says *where* on the piece it happens, and a multi-piece join that does not is reported. | Knowing that the ear attaches to the head does not make a toy; knowing it attaches across rounds 4 to 6, six stitches either side of centre, does. A pattern that omits placement leaves the maker to reverse-engineer it from the photograph, which is the "beauty image, guess the instructions" failure the Execution Directive forbids, arriving through the back door of an unspecified assembly step. Placement is validated against the piece it attaches to: a join at round 40 of a seven-round head, or two ears that would overlap at the centre, are errors that every other check in the system would pass, because the pieces themselves are correct. | No |
| B-079 | 2026-09-18 | No amigurumi is engineered yet, because the radar has not selected one. | The capability was the gap and the capability is built. The products are not: on 2026-09-18 every one of the top fourteen concepts is Class A, and the best amigurumi is Class B with a Christmas window that has not opened. Building one now would be product work the demand model did not ask for, which is the same mistake as building to a hunch. | No |
| B-080 | 2026-09-18 | A name that claims a technique must be backed by stitches that work it. | Three products were named for techniques their patterns could not contain: "Heirloom Cable Throw", "Bobble Floor Pillow" and "Chunky Ribbed Scarf", all plain single and double crochet colourwork, one of them fourth in the selected portfolio with a certificate and a drafted listing in production. This is the flat-panel-called-a-basket defect in another dimension, and nothing could see it: the shape check looks at silhouettes, Asset Truth looks at what an image depicts, and everything about those patterns was internally perfect. The check is deliberately narrow -- only techniques this taxonomy can express -- because a check that fires on a word the system has no stitch for would be unfixable by construction. | Yes |
| B-081 | 2026-09-18 | A cable crossing is one composite stitch that consumes four and produces four, not a re-ordering of ops. | The CIR consumes stitches strictly in order, and that is the single guarantee the compiler makes. A model that pretended to work them out of order would be lying about exactly that, so the crossing is a stitch: the arithmetic stays checkable, and which pair crosses in front is a property of the stitch rather than prose nobody validated. | No |
| B-082 | 2026-09-18 | The three misnamed products are rebuilt rather than renamed. | Renaming would have been a one-line change and would have left the catalogue without a single textured product, which is a category the market intelligence rates highly. The stitches to do it honestly -- post stitches, closed clusters, crossings -- are four entries in a taxonomy that was always going to need them. | No |
| B-083 | 2026-09-18 | The customer PDF renders reproducibly, so its hash means what the code says it means. | Found in production: two renders of one certified release, three minutes apart, audited under two different hashes, because reportlab stamps the current time and a random document id into every render. Artifact bytes are not durable until object storage exists, so a purchased file is re-rendered on demand -- and if every render differs, the stored hash proves only that a render happened, not that the file a customer downloads is the one that passed the gates. Which is exactly what `assets.build` claims it proves. | Yes |
| B-084 | 2026-09-18 | The container installs the fonts the renderer needs, and a missing font is a blocking problem rather than ugly output. | Every listing image ever rendered in production had a face a few pixels tall, because Pillow's `truetype` raises and its fallback is a bitmap font that ignores the requested size -- silently, successfully, and at a size that makes a title unreadable at thumbnail scale. A dependency that is satisfied locally and absent in the image is the same class of defect as "code changed, the deployed database did not", so the fix is both halves: the Dockerfile installs `fonts-dejavu-core`, and `charts.FONT_FALLBACK_IN_USE` is surfaced as `LISTING_NO_FONT`, which blocks the frame plan. An image that cannot be drawn correctly is not published badly; it is not published. | Yes |
| B-085 | 2026-09-18 | Fabric cells are shaded by their stitch's relief, not only by their colour. | The textured designs are single-colour by intention -- a cable is a shadow, not a stripe -- and the fabric renderer coloured every cell from the colourway, so a cabled throw rendered as an empty cream rectangle. Relief is a property the taxonomy already carries in stitch height, so the shading is derived from the stitch rather than hand-painted per design, and it is applied to the overhang pass as well as the base band, because the overhang was drawn second and was erasing it. | Yes |
| B-086 | 2026-09-18 | A hero image whose fabric has no internal contrast is refused, and the check measures the fabric rather than the frame. | My own frame-level thumbnail check passed the blank hero at 78% coverage and 10.6:1 contrast while production blocked the same product at 11% and 1.4:1. Production was right and my check was measuring the title text: a frame containing a large blank subject and a legible caption reads as a legible frame. So `Frame.fabric_is_flat` asks the fabric itself whether it has at least three shades spanning twelve levels, and `LISTING_HERO_FABRIC_FLAT` blocks the plan. A check that can pass an empty image is worse than no check, because it is evidence. | Yes |
| B-087 | 2026-09-18 | Production reports the commit it is running, and reports `unknown` rather than guessing. | Every other honesty check in this system compares a claim against evidence; "the deploy landed" was the exception, and it was an assumption wearing a fact's clothes. Three idempotency layers (B-070, B-071, B-072) each cost a round of "the code is fixed and production disagrees" because the question had no answer. The container has no `.git`, so only the build environment knows the sha -- and when it does not provide one, the honest answer is `unknown`, which never matches any commit. A defaulted or invented value would let a deploy check pass against a placeholder, which is the failure mode this field exists to remove. | Yes |
| B-088 | 2026-09-18 | A rebuild distinguishes a stale listing nobody has tried from one whose rebuild already ran and was refused downstream. | The fourth time this decision has been wrong, and the first time the record was the reason it took so long to see. The cable throw's listing was stale, the rebuild detected it, enqueued the work, and `assets.build` refused the release because the imagery was blocked -- which is the system working. But the listing then stayed stale, so the transition string, the token and the key all stayed the same, and every later rebuild printed the identical "stale" line while enqueueing nothing. Only a code change can deliver a transition a downstream stage refuses, and a code change reaches shipped products by bumping `CHAIN_VERSION`; the audit record now says which of the two situations it is looking at, so "stale and untouched" and "stale and already refused" stop reading the same. | Yes |
| B-089 | 2026-09-18 | Every square of a multi-colour chart carries its yarn's letter, and a chart too small to carry one is reported rather than shipped. | Master Plan section 31 asks for colour-independent cues where practical, and this was the most expensive kind of gap: everything about the charts was correct and internally consistent, and a maker with a colour vision deficiency still could not read one. Twelve of the nineteen designs are two-colour work where the colour *is* the motif, the chart separated the yarns by hue alone, and the legend's only identifier for each yarn was a coloured square -- so there was nothing in the file that said which yarn a square meant. A sighted maker recovers from a bad chart by looking harder; this one cannot. The letter indexes the CIR's own colour order, so the chart, the legend and the written instructions cannot disagree, and it is omitted from a single-colour chart because accessibility that adds noise is not accessibility. | Yes |
| B-090 | 2026-09-18 | The cue is tested as a property against two yarns given the same hex, not against cream and pine. | A test using the real palette would pass on the luminance difference alone and go green again the day a designer picks two similar colours -- which is precisely when a maker needs the cue most. Rendering two different yarns in an identical colour is the situation a colour-blind maker is in, so if the squares still differ, something other than hue is carrying the information. | No |
| B-091 | 2026-09-18 | An owner action that asks approval for a figure derives the figure from the catalogue that exists. | The fee request was a module constant reading "about US$1.80 (CA$2.50) for nine listings" -- true when it was written, and still being shown to the owner after the catalogue reached sixteen, directly beside an evidence field that computed CA$4.48 from the real count. Everywhere else in this system a claim is derived and checked; the one place a number is *asked for* had it hardcoded, which is asking consent for one amount against a charge of another. The sentence and the evidence are now the same computation, a test asserts they agree, and another refuses the old hardcoded wording if it returns. | Yes |
| B-092 | 2026-09-18 | The owner queue de-duplicates on the requirement, not on the action's wording. | Caught by a test one commit after B-091 caused it. Comparing action text worked only while every action was a frozen string; the moment one derived its figure from the catalogue, a changed number read as a new request. The owner would have seen eight entries for seven decisions, two of them asking approval for different amounts for the same thing -- which is worse than a queue that merely grows, because it makes the queue untrustworthy rather than long. An action's wording is a rendering of the request; the requirement it belongs to is the request. A figure that moved is restated in place, so the owner sees the amount they would actually be charged and only that one. | Yes |
| B-093 | 2026-09-18 | A keyless owner action is adopted by its opening clause, not by its full text. | The first migration path matched pre-upgrade rows on the whole action string, which protects every action whose wording is unchanged and fails for the only one that had changed -- so the fee row would have been left keyless and a second row added beside it, which is the duplicate the fix existed to prevent. Production was holding exactly that row. Two properties make the prefix safe and both are asserted: no two requests share their opening clause, and no derived figure reaches into it. Verified in production: seven actions before, seven after, `owner_actions_added: 0`, `owner_actions_restated: ["listing_fees"]`. | Yes |
| B-094 | 2026-09-18 | A readiness assessment can be started on demand, like the chain rebuild. | The owner queue's wording comes from the assessment and the assessment runs daily, so a deploy that corrects what an action should say leaves the owner reading yesterday's wording until midnight. That was not hypothetical: the corrected fee figure sat undeliverable in the code while the queue showed CA$2.50 for nine listings against a catalogue of sixteen, and there was no way to ask for a fresh assessment. Same argument as the chain rebuild endpoint, same minute-keyed idempotency, and GREEN by the authority matrix: it enqueues work already on a schedule, spends nothing and publishes nothing. | No |
| B-095 | 2026-09-18 | The suite runs its files concurrently rather than making its tests cheaper. | Measured before deciding: the suite is 1133 seconds and six files are 97% of it, because each drives the full eleven-product pipeline including 2000-pixel rendering. The tempting fix -- render smaller in tests that do not examine the image -- is the one this repository has already been burned by: the blank hero passed my frame-level check and production, at the real scale, was right to refuse it. So nothing about what the tests do is changed. The files were already independent processes with their own temporary databases; they were simply queued one behind another. 1133s to 344s, identical 532 passing and identical log, and the run is now floor-limited by its longest single file, which is the correct shape for a scheduler. `JOBS=1` restores the sequential behaviour if a suite ever turns out not to be independent after all. | Yes |
| B-096 | 2026-09-18 | The shadow cycle is built once and shared by the tests that only read it, with a guard that fires. | Four of nine tests in that file were 60 seconds each, and three of them asserted different things about an identical run -- one piece of evidence paid for three times. The file is 245s to 121s. The honest part of this entry is that it did **not** reduce the suite's wall clock: 344s to 348s, which is noise, because the runner already overlapped this file with the longer one and `test_product_run.py` is the binding constraint. It is kept because it frees a core for two minutes on a four-core box where twenty-six files contend, and because the file now says what it actually proves -- not because it made anything faster. A shared fixture is how a suite rots, so the cycle is fingerprinted and re-checked before every use, and a test fires that guard, because a guard nobody has seen fire is the blank-hero mistake again. | Yes |
| B-097 | 2026-09-18 | A generated lifestyle image must be disclosed wherever it appears, and may never be the hero. | Section 6 wants a consistent brand model; sections 7 and 27 want every asset to be what it says it is, and BUILD_STATE flagged the collision to be decided deliberately rather than by default. Decided: an undisclosed generated lifestyle image asserts that somebody photographed a finished object, and for every product here that is false in the strongest available way -- zero physical samples exist, so there is nothing for such a photograph to be of. It was an error only as the hero and a warning anywhere else, which let a listing ship one in frame four; a buyer scrolling a gallery does not grade images by position. Set at full strength now, while nothing in the system generates one, because a rule written after the first image exists is a rule argued against a sunk cost. | Yes |
| B-098 | 2026-09-18 | A finding is asked whether it blocks; its severity is never compared to a spelled-out string. | Found while writing the B-097 tests, and the worst kind of defect: a check that runs, produces correct findings, and has them filtered away. `ERROR` is `"ERROR"`, and three places compared `f.severity == "error"` -- so Asset Truth could not block a listing image, the policy gate could not block listing copy, and the confidence profile counted zero asset errors however many there were. Every one of those comparisons was false by construction. Verified across all seventeen designs that the defect was latent -- nothing in the catalogue had tripped the gate -- so it disarmed a guard rather than shipping anything untruthful, and production re-derives it under the fixed gate rather than taking that on trust. The fixture is a source scan, because the bug was invisible behaviour and cannot be pinned by exercising it; scoped to the Finding vocabulary, since the CFO challenge's `note | concern | block` is lowercase and correct. | Yes |
| B-099 | 2026-09-18 | A runner that has started and not yet ticked reads as starting, not dead, for a bounded grace derived from its own timings. | Observed in production during this heartbeat: `/api/verify` reported `worker_is_alive` and `scheduler_has_ticked` failing, and the worker was fine -- it had started 85 seconds earlier and the first tick lands at `_START_DELAY` 25s plus `_SCHEDULER_INTERVAL` 60s. Every deploy therefore opened a window where the verification endpoint said the system was broken, and the operator loop tells whoever sees that it is the highest-value thing to work on, so the cost of the false alarm is a session chasing a phantom or 'fixing' something that is fine. The grace is computed from the runner's knobs rather than hardcoded, so changing an interval cannot silently make it wrong, and it expires: a worker that started ten minutes ago and never ticked still reads as dead, as does one that ticked and went quiet. This narrows a window; it does not soften the check, and the evidence says `starting: true` so a passing check is never silently passing. | Yes |
| B-100 | 2026-09-18 | The continuity export is portable JSON Lines, not `pg_dump`. | Requirement 51 asks for the company to be reconstructable "if a SaaS provider disappears", and a `pg_dump` restores into Postgres and nowhere else -- so an engine-native backup satisfies the letter of "we have backups" while preserving the exact dependency the requirement exists to remove. It also makes the proof impossible: verifying a Postgres restore needs a second Postgres, which the previous drill was honest about not having, and so it had never verified one. An engine-independent format means the application container can restore its own production export into a scratch SQLite file and check it, every day, with no second database and no credentials. The cost is size and the loss of Postgres-specific types; nothing in this schema uses one. | Yes |
| B-101 | 2026-09-18 | A restore is proved by re-exporting and comparing content hashes, never by counting rows. | A restore that dropped every timestamp, coerced a Decimal to a float or mangled a JSON column would pass a headcount unchanged, and the operator would believe they had their history back. Each table carries its own sha256 over its serialised rows; `prove_restore()` restores into an empty database, exports it again and compares digest for digest, which only matches if the values themselves survived. This is the same argument as the reverse compiler: the check has to be able to disagree with the thing it is checking. A failed proof raises a P1 incident, because it means the company's unrecoverable history is not actually recoverable, which is not a log line to scroll past. | Yes |
| B-102 | 2026-09-18 | The export endpoint is closed to everyone when its credential is unset. | This is the only endpoint in the system that can return database contents, and the owner's Build-2 authorization put the constraint above the feature: no secret through logs, repository files or unauthenticated endpoints. Open-until-configured is how a backup endpoint serves an entire company to the internet during the window between deploying it and remembering to set the variable -- the window in which nobody is looking, and the one where the mistake is invisible because everything works. So an absent token means 503 for everybody including the owner, a token below 24 characters counts as absent rather than as a weak-but-valid secret, comparison is constant-time, and a refusal says only that it was refused. The token lives in the hosting environment and never in this repository. | Yes |
| B-103 | 2026-09-18 | An access request that cannot be bounded cannot be constructed. | #223 says the owner's stated willingness is not blanket authorization for unbounded spending or credentials, and the way that rule dies is not defiance -- it is a request phrased as "approve model access" with no number, which the owner reasonably approves because it sounds small. So a recurring cost with no monthly ceiling raises at construction time, and the ceiling is the figure the existing SpendLimit enforces after approval rather than a promise to be careful. The guard is in the type because a rule that lives in a docstring is one a future session writes around at 3am with a good reason. | Yes |
| B-104 | 2026-09-18 | The capability requests join the one owner queue rather than a Build-2 queue beside it. | Section 14 says a single consolidated owner queue and the reason is arithmetic: two queues means the owner reads whichever one they remember, and the one they forget is the one holding the free fifteen-minute action that unblocks the mandate. The cost is that capability requests now flow through the launch readiness job, which assesses something else -- accepted, because they are queued without moving `readiness.ready`, so `/api/launch` still answers only the question it was built to answer. | Yes |
| B-105 | 2026-09-18 | Evidence is graded by whether the capability existed, never by what the producer called it. | #224 forbids claiming continuous benchmark scanning from search snippets or stale cache, and the honest-looking version of that failure is a fixture labelled `browser_traversal` written while no browser exists. Grading on the label would bless it. So a mandated kind produced without the capability is downgraded to supporting, which means a piece of evidence cannot promote itself, and the only way to produce mandated evidence is to have the capability the owner granted. Supporting evidence is still kept and still true -- it just cannot close a requirement. A copied competitor photograph is not graded at all; it is refused, because no amount of evidentiary value makes it permissible. | Yes |
| B-106 | 2026-09-18 | The benchmark mandate is asked for as the free API route first, the paid browser second. | The spec mandates cloud browser traversal and it would have been easy to open with a CA$50/month service, which the owner has said they are willing to approve. But Etsy publishes a read-only keystring for public shop and listing data that needs no shop, no KYC and no money, and it is the platform's own sanctioned route rather than one that argues with anti-bot controls -- and it covers resolving the shop, enumerating the catalogue and reading listing text, price, tags and gallery URLs. Asking for the paid thing first would have spent the owner's trust and their money on something the free path may cover. The request presents both, with declining as a real third option, and nothing claims the API has been called: it is documented, not verified. | Yes |
| B-107 | 2026-09-18 | A benchmark that resolves to a different seller is refused, never followed. | #206 says resolve the shop's current canonical location if Etsy changes routing, without silently substituting a different seller, and those two clauses fight: following redirects satisfies the first and breaks the second. A 404 and a redirect that lands somewhere plausible are the same event to a naive follower, and the second one re-points the whole mission at a stranger while every report continues to say MJsOffTheHookDesigns. So resolution succeeds only when the destination still carries the same shop segment; a different seller and a redirect to a search page are both `wrong_shop`, which is recorded as scan health and never written to `canonical_url`. A genuinely moved shop is followed, because that is the case the requirement was written for. | Yes |
| B-108 | 2026-09-18 | An unmeasured competitive dimension is `unknown`, and parity is a finding. | The default matters more than the comparison. A company that has never observed its benchmark, defaulting every dimension to parity, reports itself level with the seller the owner named -- on exactly the dimensions nobody has examined, and in the one report the owner reads to decide whether there is work to do. `unknown` is uncomfortable to look at, which is the property that makes it useful. | No |
| B-109 | 2026-09-18 | A coverage gap cannot leave the queue without a reason, and cannot skip a lifecycle state. | #314 lists 'not pursuing + reason' as a state rather than an absence, and the reason is the requirement. A queue whose items can be closed silently shrinks by forgetting, and a coverage report then improves without the coverage improving -- which is the same class of error as counting drafted listings as published. The transition table exists for the mirror case: a gap that jumps from uncovered to launched makes the queue a claim rather than a record, and the states are supposed to be the record of what was actually done. | Yes |
| B-110 | 2026-09-18 | A score records how much of itself is evidence, and omits what it does not know. | The alternative is filling an unknown component with a midpoint, which is an opinion wearing a number's clothes and indistinguishable from a measurement the moment it is in the table. The gap queue decides where the company's scarcest resource -- finished, certified products -- gets spent, so a 0.8 computed from one component out of seven must not rank alongside a 0.8 computed from all of them. `evidence_weight` is that distinction, stored beside the score. | No |
| B-111 | 2026-09-18 | The rule against publishing competitor photography lives at provenance, not at review. | #305 permits studying MJs photography as market evidence and forbids publishing it as Brambleloop creative, and those two states are the same file. Provenance is the only point in the pipeline where a file's origin is stated at all -- by the time an image is in a gallery it looks like any other image, and every later check is about what the image depicts rather than where it came from. So a source outside camera/twin/generator/designer blocks publication, and provenance text describing a downloaded, scraped, screenshotted or recoloured competitor image blocks it by name. | Yes |
| B-112 | 2026-09-18 | Customer make-time is derived from the digital twin, never typed onto a product. | This company already refuses to let anyone assert a finished size: it is computed from the twin, and a claim that disagrees is blocked, because a typed dimension is the failure mode that ships a blanket the wrong size. Make-time is the same kind of number and decides something larger -- whether the customer is holding a finished object on the day. So it comes from stitch count, colour changes counted in working order, and per-piece finishing, which means it updates itself when the design changes and cannot drift away from the pattern it describes. The rate it divides by is an assumption and is labelled as one. | Yes |
| B-113 | 2026-09-18 | A figure is measured only through `Assumptions.measured()`, and every plan carries which of its inputs were. | Six of the seven terms in the launch-date chain are currently guesses, and the seventh is arithmetic. A dashboard that rendered a date built from guesses identically to one built from a season of sales data would do damage in both directions: the measured one gets treated with the caution due a guess, and the guess gets acted on like evidence. Making measurement a constructor rather than an assignment means a number cannot graduate by being overwritten in a later refactor, and `fully_measured` stays false until every term has actually been observed. | No |
| B-114 | 2026-09-18 | Calibration with no samples reports that it has none rather than returning a plausible rate. | `calibrate_from_samples()` is the join between two things Build 1 already had and never connected: the physical test intake asks testers for hours, and the twin knows the stitch count, so the stitch rate is a division nobody had performed. The temptation in an empty database is to return the assumed rate silently, which is how a `measured` label ends up on a guess -- the exact confusion B-113 exists to prevent, arriving through the back door. So it returns the assumptions unchanged and says in the report that no completed test has reported hours. | Yes |
| B-115 | 2026-09-18 | A product that missed its window is never launched on its seasonal premise. | #297, and the pressure against it is real: the engineering is finished, the asset is rendered, the listing is written, and shipping it costs nothing. Except that the customer cannot finish the object before the event, the listing implies they can, and the conversion data that comes back teaches the shop that the whole category does not sell -- so the cost is paid twice, once by the buyer and once by next year's plan. `recommendation()` cannot return `launch` after the latest effective date; it returns pivot, simplify or hold, with the reason. | Yes |
| B-116 | 2026-09-18 | The ephemeral-storage guard gets one exemption, and it is a parameter at the call site. | Production dead-lettered the continuity proof three times: `BRAMBLELOOP_REQUIRE_POSTGRES=1` refuses to let this company run on a SQLite file a restart destroys, and the restore proof deliberately builds exactly such a file to restore into. The guard was right and the call site was wrong. The fix is `Database(..., scratch=True)` rather than an environment variable, because a variable would switch the exemption on for the whole process -- including the application's own database, which is the failure the guard exists to prevent -- while a parameter has to be typed at each place a throwaway is genuinely meant, and greps. Verified with the flag set: the application path still refuses, the proof runs. | Yes |
| B-117 | 2026-09-18 | The benchmark library is quarantined in code, and the quarantine was built before any file existed. | The owner will buy about ten competitor patterns, which is legitimate and ordinary, and it puts a folder of somebody else's copyrighted instructions on the same disk as a system that writes crochet patterns. A `.gitignore` entry only stops the accident it happens to match. So `retrieve()` is the only reader, it refuses every generation, listing, support, marketing and publishing role by name with the reason, it refuses any role not explicitly an analyst -- closed by default, as everywhere else here -- and it refuses a path resolving outside the root, because a quarantine whose reader accepts `../../src` is a file-serving endpoint with a misleading name. Built now rather than when the files arrive, because a boundary added afterwards was absent exactly when it mattered. | Yes |
| B-118 | 2026-09-18 | Intake reads filenames and hashes, never file contents. | #170 asks the system to work out what it can so the owner does not describe forty files by hand -- and the obvious implementation parses the PDFs, which means the manifest builder is the one component that reads what the whole module exists to keep unread. Filenames carry the file's role, the filesystem carries size and digest, and between them they answer everything the manifest needs. The digest also means the manifest can prove a file is unchanged without anybody ever having opened it. | Yes |
| B-119 | 2026-09-18 | A tie in the composite standard keeps every contributor rather than the first one recorded. | Found by a test that failed for the right reason. On a 0-5 scale across twelve dimensions and ten benchmarks, ties are ordinary, and `>` silently awards them to whichever product was torn down first -- so the standard drifts toward the first purchase, which is exactly the single-source failure #162 exists to prevent, arriving by accident rather than by choice. Keeping every tied contributor is also the more useful answer: two shops reaching the same bar by different mechanisms gives the company two mechanisms to learn from. Ordering within a tie is alphabetical rather than insertion-ordered, so the composite is reproducible. | Yes |
| B-120 | 2026-09-18 | The Etsy v3 API key is `keystring:shared_secret`, and the owner was right that we had it wrong. | Verified against Etsy's own authentication documentation before changing anything, because taking a technical correction on faith is how the second bug gets written to fix the first. The docs say it outright, and `integrations/etsy.py` had been sending the keystring alone since Build 1. Latent, because nothing has ever called Etsy -- but it would have failed on the first real call with a 401, which is the single least informative failure available: every explanation for a 401 is plausible, so it costs a diagnosis round rather than naming itself. A missing secret is now sent as an empty half rather than silently dropped, so Etsy refuses it and the error points at the variable. | Yes |
| B-121 | 2026-09-18 | The mission's endpoint list is an allowlist, not a policy. | The owner's condition was to request the minimum access the read-only mission needs and not to acquire write scopes early. "We only use the public endpoints" is a sentence, and a sentence does not stop the commit that adds one more when somebody is three hours into a feature. So `ALLOWED` is nine endpoints, every one marked api_key-only in Etsy's published OpenAPI specification, a call to anything else raises before a request is constructed, and the header map has no Authorization key -- there is no code path that could send a token even if one existed. | Yes |
| B-122 | 2026-09-18 | The monthly model ceiling is checked before the call, not reconciled after it. | The owner approved a maximum, and a ceiling enforced after the fact is a report about an overspend. `check()` sums the month's ledgered `llm` cost and refuses a call whose estimate would cross CA$25, which also means every model task must declare a token estimate -- an undeclared task cannot be routed, because its cost cannot be checked. There is deliberately no override parameter: the way past the ceiling is the owner raising it, with measured usage and the capability gap attached, which is a different conversation from a boolean in a function signature. | Yes |
| B-123 | 2026-09-18 | The analysis cache is keyed on content plus the routed model. | The owner asked for caching, and the obvious key is the content fingerprint alone -- which is wrong in one specific and flattering direction. A later cost decision that downgrades gallery observation from Sonnet to Haiku would keep serving the answers Sonnet gave, so the quality would appear unchanged and the saving would appear free. Keying on the model means a routing change misses the cache and is measured honestly. Content keying also means the cache cannot go stale: a changed listing has a different fingerprint and simply misses. | Yes |
| B-124 | 2026-09-18 | An at-risk seasonal window is an incident; a missed one is not. | The daily sentinel could raise on everything late, and within a week nobody would read the channel -- which costs the alert that mattered. At-risk is the last window in which reallocating effort changes whether a customer can finish the object in time, so it is the only state where an interrupt buys anything. A missed window already has a decided answer (#297: pivot, simplify or hold), so it belongs in the report rather than in the incident queue, and it is recorded per product with its recommendation. | Yes |
| B-125 | 2026-09-18 | The CA$5K probability is a minimum over critical layers, never a weighted average. | #274 says the modelled probability cannot exceed the weakest critical layer, and the reason is a specific failure shape: a company that can design, engineer, photograph, price and schedule beautifully, and cannot get anybody to buy, has six strong layers and one fatal one. A weighted average reports that company at 0.85. It is also the arrangement that rewards building, which is what this system is good at and what #230 explicitly forbids rewarding. The minimum means the only way to raise the number is to fix the thing that is actually broken. | Yes |
| B-126 | 2026-09-18 | Every layer measured from orders is bounded by the order count in the ledger. | Several rungs need counts from systems that do not exist yet -- attribution for acquisition loops, repeat tracking, portfolio roles -- so they are caller-supplied, and that is the hole every scoring model has. Without the bound, a caller could report a conversion sample of ten thousand visits and a perfectly diversified portfolio into a database holding no sales, and the ladder would believe all of it. Conversion, order value, repeat and resilience are all *measured from orders*, and the order count is the one figure that comes from the ledger rather than from an argument, so it bounds them. The only way to raise this model is to sell something. | Yes |
| B-127 | 2026-09-18 | An observed rate is shrunk toward the pessimistic prior by its own sample size. | The alternative is that the first lucky week sets the company's expectations for the year: two visitors and one sale is a 50% conversion rate, and treating it as one produces a plan that needs a tenth of the traffic it really needs. Shrinkage means a rung earns confidence by accumulating observations rather than by getting lucky early, and it degrades gracefully -- a small sample is not rejected, it is simply worth little. Full weight at a thousand visits for conversion and a hundred orders for value, both stated in the evidence rather than buried. | No |
| B-128 | 2026-09-18 | The 75% gate is six counts, and the model is capped at 0.74 until all six exist. | #275 lists what a >=75% claim requires, and a list of prose conditions is a list somebody satisfies by argument. As counts -- four selling SKUs, two families, forty outside customers, sixty orders, two acquisition loops, three months of history -- it is satisfied by rows or not at all. The ceiling is a gate rather than a cap, and that is deliberate: a model that could only ever report near zero would stop being read, and then the honest number would go unseen for the opposite reason. | Yes |
| B-129 | 2026-09-18 | Every revenue scenario states contribution as well as gross. | CA$5,000 gross is about CA$4,430 after Etsy's 6.5% transaction fee, payment processing, listing fees and a modest refund rate -- and the owner banks in CAD while the marketplace prices in USD. A plan that tracks gross alone reaches its number on paper and misses it in the bank, and the gap is eleven percent, which is large enough to matter and small enough to go unnoticed. Required visits are computed only on the new-on-platform share, because repeat buyers and off-platform orders do not arrive through the search funnel: counting them inflates the traffic requirement and makes every path look impossible. | Yes |
| B-130 | 2026-09-18 | Every scheduled cadence is run, not just checked for wiring. | Two cadences died on their first production run in one session -- one built a scratch database the ephemeral-storage guard refused, one constructed an `Incident` with a keyword that model does not have -- and the existing cadence test passed both times, because it proves an agent may run a job type and a handler is registered, which is wiring. A handler that raises on the empty database every first boot starts from is invisible to that check. So each cadence is now enqueued and executed against a seeded database, with exactly one exemption: the shadow-mode publication refusal, which the test asserts must be present rather than merely tolerating, because its absence would mean Gate F had stopped working. | Yes |
| B-131 | 2026-09-18 | A test that can pass without entering the branch it is about is a test that has not been written. | The seasonal sentinel's test asserted over the incident list and passed while the handler raised in production on every single run, because on the day it was written nothing in the fixture was at risk and the list was empty. `all(...)` over an empty sequence is true, which is the specific shape of this mistake. The fix is not a better assertion, it is a fixture that guarantees the branch: the test now picks a date five days inside a real product's at-risk window and asserts exactly one incident with its slug and runway. The handler gained an `as_of` input to make that possible, which an operator can also use to ask what the war room looked like on any day. | Yes |
| B-132 | 2026-09-19 | The observation pipeline is built and tested before the credential exists. | The owner approved the Etsy application and the key is not set yet, and the standing rule is to route around a blocked integration rather than wait on it. The stronger argument is what the alternative costs: a pipeline written the day the key arrives is a pipeline whose first debugging session happens against a live marketplace, where a pagination mistake is a rate-limit ban and an unchanged-detection bug is a bill. Driven by an injected reader, every branch -- baseline, unchanged, new, changed, unreadable gallery, no credential -- is exercised now, and the day the key lands the scan either works or fails for a reason that is about Etsy rather than about us. | Yes |
| B-133 | 2026-09-19 | The change fingerprint covers what moves commercially and deliberately excludes what moves hourly. | Fingerprinting every field Etsy returns is the obvious implementation and it destroys the feature: `views` changes constantly, so every listing would read as changed on every scan, every gallery would be re-opened, and the cost model that makes six-hourly checks affordable would collapse -- while the code still looked like it had change detection. The fields chosen are the ones a buyer would notice: title, description, price, tags, materials, state, last-modified and favourites. The test asserts on the number of gallery calls rather than on the rows, because the rows are identical whether or not this works. | Yes |
| B-134 | 2026-09-19 | An uncovered arena is scored only on what the scan observed, with the missing components left absent. | The gap queue decides where the company's scarcest resource -- finished, certified products -- gets spent, and a scan can genuinely observe demand proxies and portfolio fit while it cannot observe make time, contribution or creative potential. Filling those with midpoints would produce a fuller-looking score that ranks arenas by an opinion, and once in the table an opinion is indistinguishable from a measurement. Leaving them out makes `evidence_weight` read under 0.4, which is the honest description of a ranking built from two components out of seven. | Yes |
| B-135 | 2026-09-19 | Palette is not a term in the concept distance function. | #84 excludes superficial colour and name variations, and the only way to enforce that is to make a recolour score zero. Including palette would let twenty colourways read as a diverse field, the tournament would pass, and every report would show a healthy creative process producing one product. Rewriting the premise cannot rescue an identical structure either: the premise term is capped at 0.15, so a new sentence over the same form, construction, motif, recipient, occasion and function still lands inside the clone radius. | Yes |
| B-136 | 2026-09-19 | Critics may only reject, and one rejection ends the concept. | A scoring jury averages the fatal objection away: five approvals and one "this is our fourth throw" produces a healthy number and the fourth throw ships. Same arithmetic error as averaging the CA$5K confidence ladder, and the same fix. It also makes the autopsy useful -- every death has exactly one named cause, so the aggregate says whether the field has a generation problem or a brief problem, which are different repairs. | Yes |
| B-137 | 2026-09-19 | Structural cleanliness is never approval; the best verdict without a vision model is `needs_taste`. | The structural critics are mechanical and real, and not one of them can tell whether a product is desirable -- that needs eyes. The tempting shortcut is to let a structurally clean concept through to engineering, which is precisely what #83 forbids: engineered because it filled a category slot. So the gate has three verdicts, not two, and the middle one is honest about who has not looked yet. It also means the model credential unlocks a capability that is already wired rather than one that must then be designed. | Yes |
| B-138 | 2026-09-19 | The creative defect is measured from the compiled CIR, not from the concept mapping. | Auditing our own catalogue through the concept vocabulary involves choices -- which slug implies which form, what a missing field defaults to -- and a conclusion resting on those is arguable, which is the last thing a defect report should be. So the audit also compiles all eleven products and reports what came out: every one is a single-component flat-rows panel using exactly two stitches. A garland, an ornament, a wall hanging, a table runner and a baby blanket are the same object at different widths. That finding survives any disagreement about the mapping. | Yes |
| B-139 | 2026-09-19 | A self-improvement hypothesis is refused at proposal time, not reviewed afterwards. | Every number this company reports is produced by a check, so the cheapest way to improve any of them is to loosen the check. "Relax asset truth so more assets pass" raises the pass rate immediately, permanently and measurably, and from inside the loop it is indistinguishable from fixing a defect: a change, then a better number. Reviewing afterwards does not work either, because by then somebody has built the thing and the argument becomes about the work rather than the rule. So the refusal happens before any effort is spent, against named capabilities rather than file paths -- a path blocklist is evaded by a refactor. | Yes |
| B-140 | 2026-09-19 | An undeclared surface is itself a refusal. | The obvious evasion of a protected-gate check is to propose "loosen the validation a little" and declare nothing, leaving the list with nothing to match against. Requiring the touched surfaces to be declared whenever the hypothesis reads as loosening makes the protected list operative rather than advisory, and costs an honest proposal one field. | Yes |
| B-141 | 2026-09-19 | The baseline is captured when the hypothesis is proposed, and a first measurement is not a win. | A result compared against a number taken after the change is compared with itself. Every promotion then succeeds, the capability history rises smoothly, and the loop is decorative -- which is the failure mode of most improvement processes rather than an exotic one. So `propose()` records the baseline and `test_result()` refuses a hypothesis that had none, converting that first measurement into the baseline for next time. It costs one cycle and buys the meaning of every cycle after it. | Yes |
| B-142 | 2026-09-19 | A degrading promotion reverts itself rather than raising an alert. | An alert is a request for somebody's attention, and the period when a promoted change is quietly degrading a metric is exactly the period when nobody is looking -- overnight, over a weekend, during the seasonal run that made the change seem urgent. The rollback reference already exists as a precondition of promotion, so reverting is mechanical. The post-promotion observation is recorded either way, because a revert that erased its own evidence would hide the pattern of what keeps failing. | Yes |
| B-143 | 2026-09-19 | Compounding is measured by lessons acted on, never by lessons recorded. | #101 asks that the company stop starting from generic intelligence, and a lesson count is the metric that makes filing look like learning: it rises every time somebody writes something down and never falls when nothing changes. Acting on a lesson is recorded against the lesson by the cell that acted, a cell that was never routed one cannot claim it, and lessons sitting routed and untouched past thirty days are surfaced as exactly that. | Yes |
| B-144 | 2026-09-19 | Every dashboard block is guarded so one unhappy subsystem cannot take the page down. | The owner asked not to have to infer progress from code logs, and a command centre that returns a stack trace because the seasonal engine raised tells them nothing about the twelve subsystems that are fine -- while failing at exactly the moment they most want to look. Each block renders its own failure in place and the rest of the page survives, which is also how the launch-readiness block already behaved; this generalises it rather than inventing it. | No |
| B-145 | 2026-09-19 | The seasonal calendar rolls, so an event is never discovered late. | #286 objects to one-off strike teams, and the reason is what a shop with them does: Christmas gets attention in October, Easter gets none until March, and the eleven months between are spent finding out that the next window closed during the last event. On a rolling calendar every event is always some number of days away -- Christmas is 364 days out on Boxing Day rather than vanishing -- and the phase its distance implies decides what work is useful today. The phase boundaries come from the lead-time engine's own arithmetic rather than from taste, so the two cannot disagree. | Yes |
| B-146 | 2026-09-19 | A trend's half-life caps the make lane it may justify, as a refusal rather than a penalty. | Ranking a flash trend lower still lets it win when nothing else is queued, and the cost of losing that argument is asymmetric: weeks of flagship engineering on something that expires before a customer could finish it loses the work and the window. A ceiling per half-life is mechanical, and an unclassified trend is refused outright, because "unclassified" is how a trend gets whatever effort somebody felt like spending. | Yes |
| B-147 | 2026-09-19 | Seasonal depth is measured against the departments an event spans, not against a product count. | "Six Christmas products" is the metric that makes a catalogue of five blankets and a coaster look like range. Each event declares the maker-and-gift ecosystem it actually spans -- a Christmas spans nearly everything, a Valentine's does not -- and coverage is the share of those departments with an answer. Measured today: ecosystem depth 0.07, and Christmas has nothing in garments, stockings, ornaments, bags, hats or seasonal gift. | Yes |
| B-148 | 2026-09-19 | A growth loop's strength is derived from recorded traffic, and the CA$5K acquisition rung reads the registry rather than a caller. | Found by a test that broke for the right reason. The registry began as a module constant listing every loop this business could plausibly run, and capping the acquisition rung against it was correct in direction and wrong in substance -- a constant cannot become true, so the rung could never rise however much traffic arrived. Loops are now rows: strength is `untested` until traffic is recorded, `attempted` below the sample floor, `measured` at two hundred visits and `repeatable` only on being seen again. A caller may still pass a number and the rows cap it. Without this, listing seven channels would have raised the modelled probability by writing down ambitions. | Yes |
| B-149 | 2026-09-19 | An experiment's design decides what its result may claim, and pre/post may never claim cause. | #266 asks to distinguish incremental revenue from revenue that would have happened anyway, and the honest implementation is not a caveat in a report -- it is a field on the record. Holdout and staggered rollout support a causal claim; pre/post cannot separate the change from the week it happened in, and in a seasonal gift business the week is frequently the largest effect. So a pre/post result is recorded as association in its own verdict text, where the next person to read it cannot miss it. | Yes |
| B-150 | 2026-09-19 | Thresholds and sample floors are fixed before the experiment runs, and a concluded experiment cannot be reopened. | Choosing afterwards what would have counted as success makes every experiment succeed, and it does not feel like cheating, which is what makes it the common failure rather than an exotic one. The sample floor matters as much: an experiment with none concludes on its first data point, and stopping early at a good number is the most reliable way to find an effect that is not there. Both are refused at construction, and feeding a concluded experiment more data until it says something nicer is refused too. | Yes |
| B-151 | 2026-09-19 | The weekly constraint is ranked by gain per week of effort, not by distance from target. | The largest shortfall is usually the slowest to close -- qualified visits is three weeks of work, conversion is one -- so ranking by gap sends agents confidently to the wrong place. Ranking by expected monthly contribution per week makes the comparison the one that actually decides where a week should go. With every term at zero the solver says something different and more useful than a ranking: the product of zeros cannot be moved by one term, and this is not an optimisation problem yet. | No |
| B-152 | 2026-09-19 | Swarm fan-out is bounded by spend, never by a headcount constant. | #174 forbids artificial scarcity and #188 requires central budget control, and those are one requirement seen twice: the ceiling that matters is money. A constant is wrong in both directions -- too small during a catalogue baseline, and expensively awake on a quiet Tuesday -- and it also hides the real constraint, so nobody learns that the limit was ever financial. Four hundred open items justifies fifty specialists at CA$25 remaining and eight at CA$0.40, and the answer says which bound applied. | Yes |
| B-153 | 2026-09-19 | A priority band is never traded away for value or urgency. | The tempting implementation collapses band, deadline and value into one score, and then a CA$5,000 opportunity with a one-day deadline jumps ahead of a customer whose download is broken. The ordering between bands is a commercial argument -- trust that cannot be re-earned, windows that cannot be reopened -- and the scheduler does not get to re-open it. Deadline and value decide within a band, scaled to stay below one band step. | Yes |
| B-154 | 2026-09-19 | An idle agent takes a standing backlog rather than polling or stopping. | Polling to satisfy a 24/7 claim spends money to produce a heartbeat, and stopping makes the claim false. The third option is the only honest one, and it only works if the backlog is real work ordered by what the business needs -- benchmark change detection first, housekeeping last -- rather than a list of ways to look busy. | No |
| B-155 | 2026-09-19 | Thrash detection keys on the call and the result together. | #34 says three materially identical states without progress. Keying on the call alone would stop a poll whose answer is changing, which is progress; keying on the result alone would stop two different calls that happen to agree. The pair is what distinguishes a loop from work: an agent making the same call and getting the same answer three times has stopped working and started spending, and the answer is to re-plan rather than to wait longer. | Yes |
| B-156 | 2026-09-19 | Nothing becomes the canonical model by being first. | The failure mode is quiet and expensive: generation produces a face, it goes into an asset because it is the only one available, the next asset reuses it for consistency, and a hundred listings later the company has a recurring model nobody chose and cannot change without reshooting everything. So candidates and canonical are separate states with no path between them that does not pass through the owner, `canonical()` returns None today, and replacing an existing canonical identity is refused even with approval -- approving a selection is not approving a redesign. | Yes |
| B-157 | 2026-09-19 | A drift check with no reference pack reports `unavailable`, never `pass`. | This is the same shape as the evidence grading in the benchmark mission, and it matters more here because the failure is invisible: identity drift is gradual, and a buyer notices a gallery where the face keeps changing without being able to say what is wrong. A check that returned `pass` for want of anything to compare against would be believed, and would wave through exactly the assets it exists to catch. An unmeasured dimension counts as maximum drift for the same reason. | Yes |
| B-158 | 2026-09-19 | A gallery frame is judged by the job it was given, and a frame with no job cannot exist. | #78's insight is that a gallery does two different things and one frame cannot do both: the hero creates desire, the later frames substantiate the purchase. Judging every frame against one blended standard produces galleries that are all evidence -- a specification sheet nobody clicks -- or all dream, which converts and then refunds. Declaring the job also makes #80 mechanical: a frame with no commercial job is filler by definition, so it is refused at construction rather than counted toward a target. | Yes |
| B-159 | 2026-09-19 | No rung of the creative escalation ladder lowers the standard, and the last one is to hold the listing. | #77 says the visual standard does not drop because an asset was AI-assisted, and a ladder ending in "accept the best available" would quietly contradict it every time the tooling was weak. The rungs go cheap to expensive -- tighter brief, new composition, different tool, deterministic render, physical sample -- and then stop. Holding a listing is a real outcome rather than a failure of the process: a listing held is a listing that did not lie. | Yes |
| B-160 | 2026-09-19 | A size a motif cannot tile is refused, never rounded. | Found the first time the grader was run against plausible numbers: a twelve-stitch motif at 16 sts/10cm cannot express a 2XL within two centimetres of its target. Rounding to the nearest count is the obvious implementation and produces a garment with half a motif at one edge -- which compiles, passes every stitch-count check in this system, and is a visible defect on the finished object. That combination is the worst available, because nothing catches it before a customer does. The refusal names all three honest fixes (narrower motif, different gauge, fewer sizes) so it is actionable rather than merely negative; four-, six- and eight-stitch motifs all close the same run. | Yes |
| B-161 | 2026-09-19 | A graded size run must be monotonic in every dimension. | One transposed number in a size table gives a 2XL narrower shoulders than an XL, and every count in it is internally consistent -- the pattern compiles, the twin agrees, the document is correct, and the garment is wrong. It is found by a customer or not at all, which makes it exactly the class of defect worth a mechanical check rather than a careful read. | Yes |
| B-162 | 2026-09-19 | Ease is declared per size rather than scaled with the body. | Scaling ease is the intuitive implementation and it changes the fit as the run grows: five centimetres is generous on an XS and negligible on a 3XL, so a constant proportion produces a small that is loose and a large that is tight while the table looks consistent. Declaring it per size makes the fit a design decision at every point in the run, and negative ease -- a garment smaller than the body -- is refused outright rather than treated as negative design intent. | Yes |
| B-163 | 2026-09-19 | A teardown audit is complete or it is not scored. | The same rule the twelve-dimension scorecard already applies to its dimensions, pushed down to the elements inside one. Scoring five of twelve chart elements and reporting the mean lets a teardown that looked at the easy things outrank one that looked at all twelve, and the mean is the number that gets quoted. Since ten purchases times nine audits is ninety schedules, the pressure to answer the quick half is real and constant -- which is exactly why it is a refusal rather than a convention. | Yes |
| B-164 | 2026-09-19 | Only the extreme scores owe a mechanism. | The opposite rule -- a justification per element -- is the more rigorous-looking one and it is why this work would never get done: 92 elements per product, ten products. A 3 is "competent; what a buyer expects" and teaches this company nothing, so it earns no essay. A 4 or 5 is a mechanism worth adopting and a 0-2 is a trap worth making impossible, and those are refused without one. The same trade #170 makes at intake: ask for what cannot be inferred, and nothing else. | Yes |
| B-165 | 2026-09-19 | A benchmark strength with no Brambleloop answer is recorded as unmatched, never converted into a requirement. | #163 refuses parity as a position, and the mechanical version of that is what a strong element becomes: a requirement to *exceed* it, naming the advantage that does. When nobody can name one, the honest output is "a competitor does something we have no answer to" -- which is the single most valuable thing a teardown produces, and the first thing an invented differentiator destroys. A row that looks finished is worth less than a row that is true. | Yes |
| B-166 | 2026-09-19 | An audit the product never promised is refused, not scored zero. | A pattern sold without video does not have bad video support; it has none, and a zero on the video dimension would make a category difference look like a defect. Across ten benchmarks that bias compounds: every seller who does not sell video tutorials reads as weaker than one who does, and the composite standard inherits it. `applies_when` reads the intake manifest, so the decision is made from what was actually delivered. | Yes |
| B-167 | 2026-09-19 | A teardown finding enters the Improvement Department through `improve.governance`, never beside it. | "Their listing states a size range the document does not grade all the way through" is a true, useful observation and a forbidden hypothesis, and only the boundary can tell the difference. Writing an Improvement row directly would have been simpler and would have made the teardown laboratory a way to launder gate-weakening as competitive necessity -- the most persuasive possible argument for it, arriving with evidence attached. The funnel also counts hypotheses that beat a baseline rather than findings filed, because #164 ends at "promote only measurable improvements". | Yes |
| B-168 | 2026-09-19 | An unrun pre-launch benchmark challenge blocks the release. | Same shape as B-157 and for the same reason: a gate that passes when its evidence is missing is not a gate, and the moment it matters most is the moment the evidence is thin. #168 exists to catch us shipping something a buyer would rate below what they already own, so "no benchmark purchased yet" cannot be the state in which it waves things through. It is carried in launch readiness as a requirement with an owner request attached, so the block is visible and has a named way out. | Yes |
| B-169 | 2026-09-19 | Portfolio *performance* and portfolio *shape* are separate modules. | Recorded because this build got it wrong: `growth/portfolio.py` was overwritten rather than extended by the CA$5K work, which would have broken the weekly `portfolio.review` cadence on its next run -- caught by the guard test before production reached it. They answer different questions -- how a SKU is performing versus what the catalogue is made of -- and neither substitutes for the other, because a catalogue of twelve healthy stars sharing one role is a portfolio one trend away from zero and every SKU in it classifies fine. The general lesson is narrower than "do not overwrite": a module whose name describes a noun will attract a second meaning of that noun, and the second meaning belongs in a second file. | Yes |
| B-170 | 2026-09-19 | An export declares its table list in the header and its completion in a trailer. | Per-table row counts caught a file cut mid-table and missed a file cut exactly on a table boundary -- every table present was internally consistent, so every count agreed and the restore reported success with tables silently absent. That is the precise failure this module exists to prevent, arriving through the one gap in the check. Found by a test that had been passing for the wrong reason until the table list grew. | Yes |
| B-171 | 2026-09-19 | A check that only runs in the failure state must be tested in the failure state. | `/api/verify` compared an aware cutoff against naive SQLite datetimes, inside a comprehension that only executes when the dead-letter list is non-empty. So the endpoint an absent owner depends on was green in every state where nobody needed it and returned 500 in the one where they did, and three tests of that endpoint passed throughout. The fix is one line; the lesson is that testing an endpoint's healthy path tells you nothing about the branch it exists for. | Yes |

| B-172 | 2026-09-19 | The culture engine's rights router was built before anything else in the package. | A culture radar is the most dangerous thing a pattern company can own, and the danger does not arrive as a decision. It arrives as a gradient: the radar optimises for demand, the strongest signals are all somebody's property, and each step downstream receives a slightly more abstract description of the same protected thing until it reaches production having been refused by nobody. Nobody chose it. So the router exists before the radar that would feed it, in the same order the benchmark library's quarantine was built before the files arrived. | Yes |
| B-173 | 2026-09-19 | Unclear rights route to the original-concept lane, never to a refusal of the opportunity. | #135 requires this and it is also the only version that survives contact with a deadline. Killing an opportunity because its obvious execution is protected teaches every downstream step that the rights gate is an obstacle, and a gate people argue with eventually loses one of those arguments. Routing to the original lane teaches them the gate is a turning -- and the turning is where a product this company owns comes from, which is #146's whole objective. | Yes |
| B-174 | 2026-09-19 | A decomposition is checked for the protected token, never trusted to have dropped it. | The protected element is the most salient thing in the input, so it is exactly what survives when the decomposition is done by judgement -- "the warmth of watching Snugglepuff" is a sincere attempt at an emotion primitive and it is the infringement intact. `check_free_of()` turns #134's instruction into a property, with word-boundary matching rather than substring, because a check that fires on "art" inside "heart" is one everybody learns to work around. | Yes |
| B-175 | 2026-09-19 | The public-domain screen refuses to compute a term from a year. | Copyright term depends on authorship, work type, renewal and jurisdiction, and a function that returned True for 1975 would be giving legal advice in the voice of arithmetic -- which is worse than no screen, because arithmetic is believed. The screen passes only works published before 1900, far earlier than any jurisdiction requires, states in the recorded evidence that passing is not clearance, and sends everything else to the original lane or to the owner. | Yes |
| B-176 | 2026-09-19 | Rights feasibility and the make-time window are gates in the opportunity score, not weights. | The same minimum-not-average rule the confidence ladder and the creative jury use, applied where the incentive to average is strongest: a signal scoring five out of five on nine commercial dimensions and zero on rights arithmetically averages to a strong opportunity, and it is a lawsuit with good margins. They multiply rather than add, and a gate that was never observed fails -- "we did not check whether we may sell this" is not a better position than "we may not sell this". | Yes |
| B-177 | 2026-09-19 | Eras and themes are a closed vocabulary; one invented per signal is refused. | An era's entire value is that it is reusable, recurs annually and belongs to nobody: retro ski lodge is available every winter forever and no rights holder can withdraw it in January. An era invented to fit one signal has none of those properties -- it is a franchise with the name filed off, and it produces the dependence #146 exists to end while looking like the cure for it. | Yes |
| B-178 | 2026-09-19 | Tournament breadth is families and distinct premises, never a count. | Fifteen ideas in two families is one idea in fifteen colours, and ten ideas whose premises restate each other is one idea typed ten times. Neither is visible to the creative jury, which reads concepts one at a time and would pass all fifteen: it is a property of the set, so it is checked where the set exists (#138). | Yes |
| B-179 | 2026-09-19 | Any one exit condition closes a cultural window, and unmeasured is not failure. | A trend dies several different ways -- interest decays, copycats arrive, conversion disappoints, the make-time window shuts -- and requiring all four is how capacity stays committed to last month's wave while the next one passes. The symmetric error is treating absent data as bad data: this company has no conversion figures at all, and an exit check that read `None` as failure would exit every trend on its first day. | Yes |
| B-180 | 2026-09-19 | The rapid-response cell's permissions are two closed lists, not a principle. | #141 says speed comes from prioritization and parallelism rather than lower standards, and a principle under deadline pressure becomes a conversation about what counts as lowering one. The pressure also runs exactly backwards from how it feels: the cell exists because a window is closing, so the argument for skipping a gate is strongest precisely when the consequence is worst -- a product rushed into a closing window has the least time left for a correction round. An unnamed gate is refused too, because a gate that can be skipped without the skip being named is already gone. | Yes |

| B-181 | 2026-09-19 | A platform policy is a dated snapshot, never a constant in the code. | A rule encoded in code is true on the day it is written and quietly becomes a claim about the past. The moment that matters is a suspension notice about listings that were compliant when they were created, and by then nobody can say which version of the rules they were checked against. Five sources are watched separately rather than as one "Etsy policy" blob, because each blocks a different workflow and a single blob blocks everything or nothing. | Yes |
| B-182 | 2026-09-19 | Never checked and unchanged are opposite states, and the watch says which. | The natural implementation reports "no material changes" from an empty table, which is the most confident possible way to be wrong. An unread source blocks the workflows it governs and blocks enabling any new asset or product class, which is #35's own requirement read literally: a new class is exactly when the old reading is least likely to cover the case. | Yes |
| B-183 | 2026-09-19 | A material policy change is a digest difference, not a judgement. | "Does this change matter?" is the judgement somebody makes quickly at the end of a week, and the answer is almost always no. Comparing digests against the previous snapshot of the same source means the question reaching a person is "this changed, does it matter" rather than "has anything changed" -- a much better question to be asked while tired. | Yes |
| B-184 | 2026-09-19 | Disclosure and misrepresentation are different problems, and a label does not convert one into the other. | The spec says never assume an AI-generated lifestyle image satisfies Etsy's listing-image requirements, and the seductive reading is that disclosure fixes it. It does not: an AI render of a finished blanket is a picture of a blanket that does not exist, and the buyer is looking at it precisely to find out what they are buying. So asset roles carry a requirement -- photograph or generated-permitted -- and a generated image in a photograph role is refused however carefully it is labelled. A deterministic render of our own chart is the exception, because it is the artefact being sold rather than a depiction of one. | Yes |
| B-185 | 2026-09-19 | Customer-use terms are five deliberate choices from closed options, rendered once. | Two ordinary failures, both prevented mechanically. The terms were never decided -- "do not redistribute" appears because it appears on everything, and then the question a customer actually has (may I sell what I make at a craft fair?) is answered for the first time in a support reply, which becomes the terms. And they drift: PDF, listing and FAQ written at different times by different parts of the system, where a buyer who finds the discrepancy may rely on the most favourable one. So the options are closed, the surfaces render from one decision, and a consistency check refuses divergence. | Yes |
| B-186 | 2026-09-19 | Decided is not enforceable, and the terms say so until somebody qualified has looked. | These are terms this company offers. Whether any of them binds anybody is a legal question, and the honest word until it is answered is "stated". `enforceable` stays False, the rendered PDF says the terms have not been through legal review, and recording a review requires naming who reviewed it and what they covered -- otherwise it is the same unreviewed terms with a more confident label. | Yes |
| B-187 | 2026-09-19 | The policy watch cadence does not fetch. | No policy reader is connected. A cadence that failed on every run because a dependency is absent would be a dead letter with a schedule, which this build has already produced twice. What does not need the network is noticing that a reading is stale or missing and opening a blocking incident for the workflows it governs -- once per source, because a blocking incident re-raised every six hours is an alert people filter, which is no alert with more rows. | Yes |

| B-188 | 2026-09-19 | A price comparison across a confounder is refused, not flagged. | The natural implementation attaches a warning and returns the number anyway. That warning is read by the person who wrote it and by nobody afterwards; the number travels on alone and arrives somewhere it is treated as a measurement. The confounders -- season, traffic source, category, sale state -- are columns rather than notes for the same reason: "was this price or was it December" has to be a filter, and a note cannot be filtered on. | Yes |
| B-189 | 2026-09-19 | An unmeasured elasticity is reported as unmeasured, never as zero. | Zero is not the absence of a claim. It is the specific claim that price does not matter, which is among the most expensive things this system could accidentally say, and it would be said by the most obvious default in the code. The same reasoning as the empty-portfolio concentration and the unrun benchmark challenge: absent evidence has its own value and it is not the neutral one. | Yes |
| B-190 | 2026-09-19 | Incrementality is measured against what the control predicts for the promoted arm's traffic. | Counting the control's own performance as a result of the spend is the entire error, and it makes every ad budget look self-funding. With one arm it cannot be done at all -- a conversion rate measured on paid traffic is a statement about who was shown the listing, and scaling on it buys more of that audience and none of the improvement. So a single arm returns "not separable" rather than a number. | Yes |
| B-191 | 2026-09-19 | A bundle is judged on contribution, and cannot be judged without a pre-bundle baseline. | The failure mode is not that the bundle fails; it is that it succeeds with buyers who were already ours and used to buy the higher-margin thing beside it. Both halves are true at once -- the bundle sold, the buyer chose it -- so nothing looks wrong, units rise and contribution is flat. Revenue is the metric that hides it and every other dashboard rewards busier, which is why the amplification check is separate and explicit rather than a number among nine that all went up. | Yes |
| B-192 | 2026-09-19 | Clicks with no saves is a product problem; saves with no orders is an offer problem. | #47's missing rung, and the two look identical in every aggregate -- both are clicks that did not become orders. The interventions are opposite: reworking price and trust on a product nobody wanted produces a cheaper version of something nobody wants, and the listing has already done its job by getting them there. Checked before the conversion stage so the cheaper diagnosis cannot claim the case first. | Yes |
| B-193 | 2026-09-19 | The class list lives in the module, and the ladder test derives from it. | Found while adding APPEAL_PROBLEM: the test enumerated classes by hand, so a new class with no intervention ladder would have passed. A hand-written list in a test passes unchanged in exactly the situation the test exists for. | Yes |

| B-194 | 2026-09-19 | The reinvestment recommendation is the envelope scaled by the modelled confidence. | The envelope is what survived the reserves; the recommendation is what the evidence supports spending out of it, and those are different numbers. Cash from an unrepeatable source is not evidence that spending will repeat it, so a company with money in the account and no customers recommends nothing -- which is the correct answer and not the one an optimistic CFO would produce. Confidence is read from the ladder rather than accepted as an argument, so a recommendation cannot be manufactured by supplying a flattering number alongside the cash. | Yes |
| B-195 | 2026-09-19 | No code path in the reinvestment module returns owner_approval_required False. | An authority boundary described in a docstring is one the next change forgets. Making every branch return True means the only way to spend is for a person to act on the recommendation, and it means the boundary cannot be eroded by an edge case somebody adds for a small amount. Small amounts are how standing budgets start. | Yes |
| B-196 | 2026-09-19 | There is no function that returns a release count without its four companions. | #52 says track releases/week only alongside defect rate, support burden, conversion and contribution, and the way that requirement is normally satisfied is a dashboard that shows all five and a function that returns one. An agent optimising its own throughput moves the number it can move without a customer, and releases per week is the cheapest of all of them. The pairing is therefore structural rather than presentational. | Yes |
| B-197 | 2026-09-19 | A companion metric moving off zero is a degradation, not an unmeasurable. | Found immediately on running the comparison: the percentage-change guard skipped every metric whose previous value was zero, which drops the single most important movement it can see -- a defect rate going from none to some. The obvious defensive `if not before: continue` was exactly wrong for the lower-is-better metrics. | Yes |
| B-198 | 2026-09-19 | A weekly review that stops nothing must say why, and is not blocked. | A hard requirement to stop something every week manufactures removals, which is worse than the bureaucracy it prevents. A silent empty list is what a bureaucracy reports every week for years. One sentence is the difference: it costs a lean week nothing and it makes an accumulating one visible. | Yes |
| B-199 | 2026-09-19 | The confidence ladder opens with an architecture rung that is earned and changes nothing. | #55 asks for confidence in stages rather than a jump from zero to a modelled band, and a ladder whose every rung reads 0.00 shows no stages at all -- it teaches an owner to stop reading it, which is the opposite of what an honest number is for. Architecture is the one layer this company can honestly earn today, it is derived from rows (a certified pattern, a proved restore, a refused publish, completed work, no accumulating dead letters), and it is non-critical, so earning it does not move the modelled probability by a point. Progress is visible and is not convertible into confidence about revenue. | Yes |
| B-200 | 2026-09-19 | CA$3,000 and CA$5,000 are separate questions, and today they have the same answer. | A model that scaled confidence with the size of the goal would report CA$3,000 as comfortably likely while no customer had ever existed. Both bands are computed independently and both report zero, because the binding layer counts customers: a smaller target is not a nearer one when nothing has been sold. Reporting them side by side makes that visible rather than leaving it to be assumed either way. | Yes |

| B-201 | 2026-09-19 | Contribution per visitor is the first-class metric; conversion rate is an input to it. | #24's example is worth restating because every instinct disagrees with it: a CA$25 pattern converting at 1.6% beats a CA$12 pattern at 2.5%, and the second listing looks healthier on every dashboard. Conversion rate is a ratio whose denominator the company is also buying, so optimising it optimises the wrong half. The ranking function reports explicitly when the two orderings disagree, because that disagreement is the requirement rather than a footnote to it. | Yes |
| B-202 | 2026-09-19 | Required visits are derived only from an observed conversion rate, never from a category benchmark. | The benchmark is available, plausible and would make the funnel complete -- which is exactly the problem. A funnel built from category numbers produces a confident plan about a company that does not exist, and it is indistinguishable in shape from a plan built from evidence. The arithmetic that does not need a rate (orders at a price) is still shown, so the honest half is not lost with the dishonest one. | Yes |
| B-203 | 2026-09-19 | The binding constraint is walked in funnel order, and the CTR check precedes the traffic check. | Caught by a test that expected "thumbnail" and got "traffic": a listing with 100,000 impressions and 200 visits satisfies "strong conversion, low traffic" and the correct action is not more impressions. Buying traffic when 99.8% of what already arrives bounces is buying more of a broken funnel, and "low traffic" is the diagnosis that would send us there. Funnel order was the stated principle and the first implementation contradicted it. | Yes |
| B-204 | 2026-09-19 | Allowable CAC is set against contribution, never revenue. | Setting it against the top line is how a business buys customers at a loss while every headline number rises -- the same failure as the bundle judged on revenue, arriving through the acquisition side instead of the catalogue side. | Yes |
| B-205 | 2026-09-19 | #27's qualitative conditions sit beside the counts, and an unstated condition is unmet. | Counts and conditions fail differently: volume arrives from one lucky listing, and the conditions are statements about the shape of the business. A shop with sixty orders from a single viral outlier, a negative contribution margin and an open policy warning satisfies every count in the gate and none of the conditions. Treating an undeclared condition as unknown rather than unmet would let the gate be passed by omission, which is the cheapest way to pass any gate. | Yes |
| B-206 | 2026-09-19 | An artefact produced by uncosted work is reported as a floor, not as cheap. | The averaging instinct is to spread unattributed spend over the artefacts, which makes every unit cost look plausible and the total look understood. An artefact produced by work nobody costed looks free, and free is the most dangerous price -- so the count of uncosted producers is reported per artefact and the unattributed spend is reported as its own number. | Yes |

| B-207 | 2026-09-19 | The brief carries the novelty, not the validator. | The measured defect is that every product in the catalogue compiles to one component shape in two stitches, and no amount of judging fixes it: given a season and a category, the highest-probability output is the commodity, because the commodity is what the category is made of. A validator can only reject what arrives. So the invention matrix, the transformation patterns, the motif grammar and the wow requirement all act on the *brief* -- the generator is asked for a specific unusual thing rather than for a good thing. | Yes |
| B-208 | 2026-09-19 | A brief built from one dimension is refused, and so is one built from two near-synonyms. | One dimension is a category, and a category is what everybody else is already making -- "a Christmas ornament" is not a premise. Near-synonyms are the same failure with two names: storage crossed with organization reads like a combination and is one idea stated twice, which is exactly what a system under pressure to produce pairings would emit. | Yes |
| B-209 | 2026-09-19 | #108's sentence is enforced by removing the title's words from the premise. | "A concept that needs its title to explain why it is interesting is weak" is unenforceable while the two are read together, because the title supplies the interest and the reader cannot tell which half carried it. Deleting the title's words and counting what specific language remains makes the test mechanical, and "Gingerbread Village Basket" / "a gingerbread village basket" leaves nothing. | Yes |
| B-210 | 2026-09-19 | An emotional promise names the physical thing that delivers it, and copy language is refused. | "Cosy" achieved by writing cosy in the listing is the failure #109 exists to name, and it is the cheapest way to satisfy any check that reads text. Requiring an execution -- structure, texture, motif, colour relationship, scale or finish -- means the promise has to survive a buyer looking at the photograph with the sound off. | Yes |
| B-211 | 2026-09-19 | A flagship's WOW mechanism must be grounded in the specific object. | "Clean and correct is not enough" is unenforceable as a sentiment and trivial as a checkbox, and a checkbox is what a generator learns to tick. Naming the mechanism from a closed list is half of it; requiring four specific words about *this* object is the half that stops "it has a reveal" counting. | Yes |
| B-212 | 2026-09-19 | The creative floor is the trailing median, with an absolute floor underneath it. | A fixed threshold is one the company grows past and keeps meeting: six months of learning produces better concepts, the bar that rejected two thirds now rejects none, and the pass rate improving looks like success. A trailing median alone has the mirror failure -- a bad quarter would lower the standard permanently -- so the absolute floor sits underneath. That is a fixed threshold arriving from the other direction, which is acceptable as a floor and not as a bar. | Yes |
| B-213 | 2026-09-19 | Boring is a verdict, and no correctness score overrides it. | #129's most important sentence is that agents must be able to reject technically valid ideas. Every other gate in this system is a correctness gate, and a concept can pass all of them and be the ninth version of something nobody remembers -- the correctness score is precisely what a boring concept passes. So the taste rejection is a closed vocabulary with no numeric content, and it ends the question rather than contributing to a total. | Yes |
| B-214 | 2026-09-19 | A rejection reason is a closed vocabulary, or forty autopsies have forty reasons and no pattern. | The useful output of a tournament that rejected forty concepts is the pattern in the forty. Free text produces forty individually reasonable sentences and no aggregate, so five of six rejections being "obvious" -- which is a brief problem rather than six concept problems -- stays invisible. | Yes |

| B-215 | 2026-09-19 | A brief is generated against a cell, and coverage is measured in cells. | "Christmas crochet patterns" produces what the phrase suggests. The same December contains a mantel stocking, a teacher gift under fifteen dollars, a nursery keepsake kept for thirty years, a table setting and a front door -- different products for different people. A catalogue ends up with six variations on a blanket because nothing ever asked which cell each one was for, and a product count cannot see it: six products against 288 cells reads as six products. | Yes |
| B-216 | 2026-09-19 | Skill level is market segmentation, not difficulty. | A wave of only flagship work has no beginner entry and no impulse purchase; one of only quick makes has nothing anybody keeps. Both look busy, which is why this is measured as shares against a healthy range rather than left to judgement -- and the useful signal is "there is no beginner product at all" rather than "beginners are 22%". | Yes |
| B-217 | 2026-09-19 | Tournament diversity is measured per axis, because the entrant count is what hides its absence. | Twelve concepts sharing one construction have explored one construction, and twelve is exactly the number that makes it look like exploration. Measuring dominance per axis -- form, function, tone, construction, motif family, recipient, make time -- means breadth in one axis cannot stand in for breadth. | Yes |
| B-218 | 2026-09-19 | A crowded archetype needs a named unmet angle before anything is built. | Forty near-interchangeable pumpkin coasters is evidence the archetype sells and evidence the forty-first is invisible, and only the second reading has arithmetic behind it. The instinct runs the other way and is nearly irresistible, so the refusal names the number: "ours will be nicer" is what everybody entering a crowded category believes, including the forty already there. | Yes |
| B-219 | 2026-09-19 | White space is mined from complaints, and an empty complaint set produces nothing rather than a proposal. | A buyer saying what went wrong -- too much sewing, sizing that stops short, a variant nobody makes -- is a product brief with a customer attached, which no search volume report is. With nothing recorded, the tempting fallback is to propose from the category, which is precisely how a white-space agent rediscovers the commodity and reports it as discovery. | Yes |
| B-220 | 2026-09-19 | The mechanism transfers between seasons; the theme does not. | #120's distinction, and the one that collapses first under a deadline, because the finished design is right there and the mechanism needs inventing again. If modular character pockets convert at Halloween, Christmas explores modular character pockets -- not the Halloween product with a hat on it. Enforced by requiring a new theme of its own and by refusing a mechanism named as a product. | Yes |
| B-221 | 2026-09-19 | A missed seasonal milestone is a portfolio failure, and the failure is silent. | A phase that slips does not announce itself; it becomes the next phase, and the first visible symptom is a product that lists in December. Ten dated milestones per occasion make the slip a row rather than a realisation. Run today, Christmas 2026 has five of ten already past -- research, concept freeze, engineering start, physical test and creative production -- with listing/indexing due in a week. | Yes |
| B-222 | 2026-09-19 | The north-star metrics report which of them can be computed today. | Four of the eleven need no customers and seven do. A dashboard showing four numbers and seven blanks is read as four numbers, so the split is stated rather than implied, and the cohort framing is deliberate: a running average of everything ever made moves too slowly to show that anything changed, which is indistinguishable from nothing changing. | Yes |

| B-223 | 2026-09-19 | The collection calendar is checked by the daily sentinel, not by somebody opening a page. | The finding that five of Christmas 2026's ten milestones were already past came from running the function once. A function nobody calls produces that finding once and never again, which is the same as not having it -- and the owner's instruction after the first seasonal finding was explicit: use the engine continuously rather than rediscovering timing manually. One incident for the worst-affected event rather than one per missed milestone, because eight rows about one Christmas is the noise that trains everybody to close the channel. | Yes |

| B-224 | 2026-09-19 | The build loop's graph, queue and parking state live in Postgres, not in a session. | Build 2 did route around the missing Etsy credential -- eight milestones landed while it was unavailable and no requirement waited on it -- but the thing that routed around it was a conversation. The dependency graph was prose in BUILD_STATE, the priority queue was judgement, and the parking decision was made once and remembered by whoever was in the session. All of that evaporates when the session does, and the failure is invisible until the moment it matters. Autonomy that depends on a particular process staying alive is not autonomy. | Yes |
| B-225 | 2026-09-19 | A task may only be parked on a condition the code can test, and an owner-gated requirement with no gate is refused. | Free text would make parking a place to put anything difficult, and a queue that can absorb its own difficulties never reports being blocked and never finishes. The refusal is the load-bearing half: without it an owner-gated requirement with no gate falls through into the ready list as work nobody can do, and the queue reports more ready work than exists -- which is the one number the whole module is for. Running this validator for the first time found four such requirements. | Yes |
| B-226 | 2026-09-19 | Two of the seven owner gates check database rows rather than environment variables. | Benchmark purchases is a BenchmarkProduct row count and advertising authority is a SpendLimit row with a positive cap. Keeping them in the same mechanism as the credential gates is deliberate: the question a gate asks is "is this true yet", not "is there a credential", and a design that only handled credentials would have needed a second, weaker mechanism for everything else the owner does. | Yes |
| B-227 | 2026-09-19 | Never-idle is measured in completed requirements, not in ticks. | A loop that always has something to do is trivially satisfiable by inventing work -- the failure the swarm's standing backlog already names for agents, and it applies with more force to a build loop because nobody is watching. The watchdog therefore asks whether anything has been *completed*, and distinguishes idle-with-ready-work (a stall, an incident) from idle-with-everything-parked (correct, no alarm). Those need opposite responses and look identical from outside. | Yes |
| B-228 | 2026-09-19 | A claim carries a lease, so a dead session loses a worker rather than the build. | The same logic the job queue already uses, for the same reason: a session that dies mid-requirement must not hold it forever. Ninety minutes, after which another worker takes it over with no manual intervention and no human noticing that one was needed. | Yes |
| B-229 | 2026-09-19 | The requirement registry lives inside the package, because the container only copies `src`. | Found by deploying the build endpoint: the registry sat at the repository root, the Dockerfile copies `src`, and so `/api/build2` had been returning 500 in production for as long as it existed while passing every test locally. Moving the data file beside its module makes it travel by construction rather than by somebody remembering a COPY line. The general guard is the more valuable half -- a test that walks the app's declared GET routes and calls each one, so a new endpoint is covered the moment it is registered. | Yes |

| B-230 | 2026-09-19 | The off-device proof counts the same dead letters its verdict tests. | Found by running it against production: the report said sixteen and the condition meant one. Shadow-mode publish refusals are the gate working, so they are excluded from the test -- and they were not excluded from the count printed beside it. A number that does not measure what the verdict beside it measures is the same class of defect as an unmeasured rate reported as zero, and it was doing it in the one report whose whole job is to be believed. | Yes |
| B-231 | 2026-09-19 | A third content boundary, drawn in a different place: generic finished-object characteristics may be preserved. | The teardown library refuses a competitor's instructions; the culture router refuses their property; this one has to permit "cropped V-neck button cardigan" while refusing "their stripe sequence". The permitted side is wide on purpose, because a rule that forbade entering a proven arena would make the intelligence mission conclude that the useful answer is never to act on what it found -- which is the most expensive possible conclusion for the company's stated top priority. Closed list of characteristics, and a refusal for anything that only makes sense as a reference to one shop's product. | Yes |
| B-232 | 2026-09-19 | "MUST consider" is satisfied by a recorded refusal and never by silence. | An obligation nobody can check is a sentence, and silence is exactly what a queue produces when nothing forces a row to exist. A declined arena with a reason somebody can disagree with later is a real answer; no row is not. Entering also requires a differentiator, which is #163's anti-parity rule arriving through the competitive door rather than the teardown one. | Yes |
| B-233 | 2026-09-19 | Seasonalising enumerates eight lenses, and a palette-only answer is reported as a recolour. | An agent told to "seasonalise" answers for the colour and stops, and a palette answer alone is precisely the output this build measured as its creativity defect. Naming the lenses individually -- styling, trim, motif, gift context, accessories, bundles, merchandising -- is what stops the easy one standing in for the question. | Yes |
| B-234 | 2026-09-19 | Every pipeline stage names its gate, and a stage cannot run before its predecessor. | A long pipeline never fails by collapsing; it fails by one stage quietly becoming optional, and a stage nobody named cannot be noticed missing. A failed gate stops the run rather than marking it amber, because amber is how a stage becomes optional in practice. | Yes |

| B-235 | 2026-09-19 | A completion is a transition this system observed, not a status it found on first sight. | Two bugs in one place, both found by watching production. Registry-driven completions -- the commonest kind, because a session finishes work and moves the status -- were recorded only as a sync event, so the watchdog reported a stalled loop while six requirements had just closed. A false alarm in the channel that exists to catch a real one is worse than no channel. Fixing that naively then made the first sync count the whole 140-requirement backlog as completions, and a table created ten seconds ago reported a busy loop. Only a transition counts. | Yes |

| B-236 | 2026-09-19 | The benchmark mission's capacity floor is reserved, not allocated. | Capacity merely allocated to a mission gets borrowed, and the borrowing decision is locally sensible every single time: generic research is busy now, the specialists are idle now, moving them is obviously right. It is obviously right often enough that the mission ends up with an org chart and no throughput, and nobody ever decided to defund it. The refusal names the five kinds of work it would starve, so the cost of borrowing is visible at the moment somebody tries. Above the floor the ceiling is money rather than headcount, which is #302 and #188 being the same requirement seen twice. | Yes |
| B-237 | 2026-09-19 | The catalogue map records judgement columns as absent with the capability named, never inferred from the title. | Silhouette, merchandising mechanism and styling are judgements about a photograph. Inferring them from a listing title would make every row look complete and be fiction -- and *persuasive* fiction, because seven of the ten columns would be right. Completeness is therefore scored against what the available capabilities can observe rather than against all ten, or a correctly limited map would report itself permanently broken. Palette is distinguished from both: it is published per-image by Etsy, so its absence is a request nobody made rather than a capability nobody has. | Yes |
| B-238 | 2026-09-19 | A map that does not know how stale it is is not living. | A catalogue map nobody has refreshed is indistinguishable from a current one right up to the moment it is wrong -- and it is wrong exactly when the shop changed something, which is the only moment anybody is reading it. Every row carries its evidence date and the map reports its oldest. | Yes |
| B-239 | 2026-09-19 | Only a recorded outcome moves a pod's learning memory; repetition is refused as evidence. | The failure every learning system has is confidence that grows with repetition. Forty observations of one shop's Christmas listings is one observation made forty times, and the interpretation built on it feels increasingly certain while nothing has been learned. So the outcome vocabulary is closed -- launches, conversion, complaints, owner vetoes, benchmark confirmations -- and 'observed again' is refused by name, with the reason attached. | Yes |
| B-240 | 2026-09-19 | A challenger wins on outcomes or it does not win, and a tie leaves the incumbent standing. | Promoting on equal evidence is promoting on novelty, which makes the memory track whoever last wrote a paragraph. A superseded lesson is kept rather than deleted: the useful question next year is not what the pod believes but what it used to believe and what changed its mind, and a memory that has only ever been right is a memory that is not learning. | Yes |
| B-241 | 2026-09-19 | The competitor-expression boundary is called, not copied. | The pods' check lived inline inside `lesson()`, and the new learning memory needed the same rule. Extracting it was the only honest option: a boundary that exists twice is a boundary that drifts, and the copy that drifts is always the newer one -- the one written by somebody who did not have the original's reasoning in front of them. | Yes |

| B-242 | 2026-09-19 | Make-time is an interval, and impossibility requires the optimistic bound to have passed. | The owner's correction, and it caught this build making the exact move it refuses everywhere else. The lead-time chain is exact arithmetic over guesses -- seven crochet hours a week, a skill multiplier, no maker ever timed -- and its single date was reported first as a fact and then as an impossibility covering a whole season. Unmeasured is not zero and absent is not inferred; a point estimate is not a proof of impossibility either. Under the interval, LONG Christmas work is high risk rather than closed, and FLAGSHIP is genuinely closed -- so the correction is not an excuse, it is a distinction. | Yes |
| B-243 | 2026-09-19 | The interval is asymmetric and only samples narrow it. | Craft overruns: a project going well finishes a little early, one going badly finishes very late -- frogged rows, a gauge surprise, a fortnight nobody touched it. A symmetric band would be a third wrong claim dressed as rigour. And the only thing that narrows it is a recorded physical test; more planning does not, which is worth stating because planning is what a system under time pressure produces instead. Even fully calibrated it stays an interval, because two people making the same object differ and a tighter band would claim to know the maker rather than the pattern. | Yes |
| B-244 | 2026-09-19 | Executable and ready are two numbers, not one. | An earlier invariant asserted the queue's ready count equalled the registry's executable count. That held only while every gated requirement was also `owner_gated`, and it stopped holding the moment a requirement was half-built with its remainder behind a credential -- the ordinary case rather than the exception. Keeping the equality would have forced a choice between two lies: calling a half-built requirement owner-gated, or calling gated work ready. The honest invariant is that every executable requirement is either ready or parked, and nothing owner-gated is ever ready. | Yes |

| B-245 | 2026-09-19 | A tester's photograph without recorded consent is refused, by the same mechanism as a competitor's. | The error arrives from the friendly direction: the tester was pleased, they posted it themselves, nobody would mind. That is exactly why it needs a mechanical check rather than good intentions -- the competitor case is obvious and this one is not, and they are the same act. A physical photograph also records the yarn it was made in, because a buyer choosing a substitute is choosing against that picture. | Yes |
| B-246 | 2026-09-19 | The digital-not-finished disclosure belongs in the title, not the description. | A buyer who thinks they are purchasing a blanket does not read the description, and that refund is the single most preventable one in the category. "It says so in the description" is true of a description nobody read, which is why the six purchase-protecting disclosures are a closed set checked against the surface each appears on. Confusion contacts are counted as a listing defect rather than a cost of doing business: counting them as a cost budgets for them. | Yes |
| B-247 | 2026-09-19 | The order-to-version map is written at sale time because it cannot be reconstructed. | Once a listing moves to 1.2.0 there is no record of who bought 1.1.0 unless somebody wrote it down at the time. A material correction then cannot say which buyers are affected, and the answer is either a message to everybody or a message to nobody. This is the one requirement here that is impossible to retrofit, which is why it exists before the first order does. | Yes |
| B-248 | 2026-09-19 | A trend datum with no population or window is refused; a foreign one is discounted. | Refusal and discount are different answers to different problems. A number that cannot say whom it measured is not weak evidence about anybody, and discounting it would imply it was. A US crochet figure is genuinely evidence about Canada -- weaker, not worthless -- so it carries a factor, and the factor is baked into the value the consumer sees rather than offered as a caveat beside it, because a caveat is read once and the figure travels on alone. | Yes |
| B-249 | 2026-09-19 | Staleness is measured against the topic's own seasonality. | A Christmas figure from last January is one cycle old and about to matter again; a two-week meme from last January is archaeology. A plain age cut-off treats them identically, and whichever threshold it picks is wrong for one of them. | Yes |
| B-250 | 2026-09-19 | Dependency impact is graded by what stops, and a dependency with no recovery is refused. | Vendor prestige gets this backwards routinely: Postgres holds the company's entire memory and GitHub holds a copy of code that is also on disk and in the running container. One is fatal and one is an inconvenience. The refusal matters more than the grading -- a hand-written map marks three things critical, and those are the three somebody was already worried about rather than the three that would end the company. | Yes |

| B-251 | 2026-09-19 | A challenger is judged on a fixed shared task set, never on tasks it introduced. | The failure that makes every other check in a league decorative. The author of a new configuration has necessarily been thinking about the cases it handles well, so a challenger allowed to bring its own tasks wins whatever the thresholds are -- and the result looks like evidence. Adding a task is a separate act that applies to everybody retrospectively. | Yes |
| B-252 | 2026-09-19 | Cost and reliability are outcomes in the league, and the cost bar scales with the ratio. | A league reporting only quality takes every expensive trade and finds the bill at the end of the month, and a configuration that wins when it answers while failing twice as often has not won. A flat cost threshold was the first implementation and it treats 1.3x and 5x as the same trade: doubling the spend may buy a real improvement, quintupling it has to buy a large one, so the required quality gain scales with the ratio. | Yes |
| B-253 | 2026-09-19 | The weakening detector matches a verb governing a gate object by proximity, not a literal phrase. | Found by writing "lower the thumbnail threshold": the phrase list caught "lower the threshold on the thumbnail check" and missed the same sentence reordered. A detector a paraphrase defeats fails exactly when somebody is rewriting a hypothesis to get it through, which is the only occasion it matters. The first fix -- any weakening verb plus any gate object in one sentence -- then refused "tightening the thumbnail check should reduce defects", a hypothesis that *strengthens* a gate. So the verb has to govern the object, judged by proximity, and both failures now have regression tests. | Yes |

| B-254 | 2026-09-19 | Testing demand is forecast from the seasonal calendar, not from the request queue. | Testing is the one constraint where wanting it faster changes nothing -- a tester crochets at a human speed -- which makes it the one discovered late. A queue of test requests tells you what has already been asked for, and the entire lead time lives in the difference between that and what the calendar says is coming. Reliability is completed against accepted, not enthusiasm: a tester who has not finished anything is not capacity, and one who has never been asked is neither reliable nor unreliable, which the report distinguishes. | Yes |
| B-255 | 2026-09-19 | A specialty covered by one tester is a single point of failure and counts as one. | A network of four people where one does every garment is a network of one for garments, and it reads as four on any headcount. The alarm is per specialty for that reason rather than per network. | Yes |
| B-256 | 2026-09-19 | The brand moat is an inventory with replication time, and the model is the most copyable entry in it. | #44's point is that the recurring model is one asset and not the brand, and the reason it gets mistaken for the brand is that it is the most *visible* asset -- which feels like the same thing as the most valuable. Measuring each signature by how long a competitor would need reframes the list: a naming architecture is an afternoon, version-aware support is structural because the order-to-version map cannot be backfilled. Replication time is what it would cost *them*, never what it cost us: an expensive thing that is easy to copy is a sunk cost. | Yes |
| B-257 | 2026-09-19 | A planned brand signature is not counted as a moat. | Listing an aspiration beside the real ones produces a page saying the company is defensible when it is aspiring, and that page is the one somebody reads before deciding not to build the thing. Five of the ten signatures exist today and the inventory says which five. | Yes |
| B-258 | 2026-09-19 | Realised benefit is never read from the sandbox result that won the promotion. | The promotion records its own result as a capability point, so the naive implementation reads it back as the return and every promotion succeeds by construction -- the most comfortable way for an improvement programme to stop being a measurement. The return is what production measured afterwards, against the baseline captured at proposal time. | Yes |
| B-259 | 2026-09-19 | A promotion younger than the realisation window is too_early, not unmeasured. | Collapsing the two makes a young programme look like a bad one, and a hit rate computed over promotions made this morning is a number about the calendar. Only changes old enough to be readable are counted in the rate. | Yes |
| B-260 | 2026-09-19 | A design that drew on no accumulated lesson is recorded, never refused. | The first product in a new territory legitimately has nothing to stand on. What matters is the catalogue: if every design cites nothing, the company is restarting from generic intelligence each time, which is what #101 is named after -- and that is only visible if the zero-lesson designs are recorded rather than rejected. | Yes |
| B-261 | 2026-09-19 | Family membership requires both that the idea still reads at the new size and that the form can be built that way. | Size compatibility alone pairs an amigurumi robin with a scarf: two small objects, and no amount of increase-and-decrease shaping turns one into the other. Checking only size invents members, which is the forced derivative #111 exists to prevent reached from the other side. A member must also differ from the hero on form, because a family whose members differ by palette is a recolour with a collection name on it. | Yes |
| B-262 | 2026-09-19 | A concept that seeds no family is reported as a single product, not rejected. | A brilliant single product is a legitimate thing to build. It is a bad thing to discover after engineering three derivatives out of it, which is the whole reason the test runs before engineering rather than after. | Yes |
| B-263 | 2026-09-19 | The shopping-window critic judges nothing when nobody said how long is left. | An unknown window is not a comfortable one, and inferring a date would fire the critic on every everyday product. Absent is not inferred, applied at the point where creative capacity is allocated. | Yes |
| B-264 | 2026-09-19 | A department whose forms are all past their launch window is next season's gap, not this season's work. | Spending the runway that is left on something nobody can buy in time is the expensive version of looking busy, and a gap queue that ignores the window produces exactly that. Hurry and stop stay in separate buckets for the same reason they do everywhere else in this build. | Yes |
| B-265 | 2026-09-19 | A brief is never counted as coverage. | A gap report that produces no work is a report, so gaps become briefs -- but a queue that counted its own briefs would report depth the catalogue does not have. Depth moves when something is built, and a test holds the line. | Yes |
| B-266 | 2026-09-19 | The continuity archive is retained in the database, and that is called surviving the container rather than durable. | The proof ran nightly on a file that no longer existed an hour later. Holding the bytes in Postgres survives a replacement, a redeploy and a crash -- the failures that actually happen -- and not the provider disappearing, which is the failure #51 names. The claim stops where the evidence does, and the remaining half is parked on a gate whose condition code can test. | Yes |
| B-267 | 2026-09-19 | An export never contains the previous exports. | Each retained archive is a copy of the export, so including them makes every night's backup carry the compressed bytes of the last three and the backup grows faster than the company it describes. The archive table is excluded, and a test asserts the export size stays flat across four generations. | Yes |
| B-268 | 2026-09-19 | Etsy's policy pages are not fetched by spoofing a browser user-agent. | A plain request returns 403 from their bot protection while robots.txt fetches fine, so the edge is refusing an automated client deliberately. Getting past it by pretending to be a browser is evasion, and doing it to a marketplace this company intends to sell on trades a small convenience for the relationship. The requirement is parked on the browser-worker gate instead. | Yes |
| B-269 | 2026-09-19 | A requirement re-audited as gated is retired from the build queue rather than left as a stale row. | sync() skipped past existing tasks whose requirement stopped being schedulable, so five requirements re-audited this session kept reporting themselves ready. A queue advertising work nobody can start is the exact failure the executor was written to prevent, arriving through its own reconciliation path. | Yes |
| B-270 | 2026-09-19 | The model-provider gate reads a recorded successful call, never a configured key. | The key the owner supplied authenticates against an account with no credit: the first request returned "Your credit balance is too low to access the Anthropic API". An environment-variable check would have opened the gate and un-parked four requirements onto work that cannot run, which is the queue advertising work nobody can start -- the failure the executor exists to prevent, arriving through its own gate definition. A probe cadence records real calls, so the gate opens by itself when credits arrive. | Yes |
| B-271 | 2026-09-19 | The monthly model ceiling is checked before the call, on a padded estimate that assumes the whole output allowance is written. | A ceiling discovered on the invoice is not a ceiling, and one that trusts an optimistic estimate is crossed before anybody notices. Budgeting for the usual case is how a ceiling becomes a target. An unpriced model is refused outright, because a call whose cost cannot be computed is unbounded. | Yes |
| B-272 | 2026-09-19 | Configuration may lower the model ceiling and may never raise it. | A ceiling an environment variable can raise is a default. The owner's CA$25 is the maximum the code will honour whatever the variable says. | Yes |
| B-273 | 2026-09-19 | Model prices are recorded as an assumption on every row they produce. | The per-token figures are the provider's published list at an assumed exchange rate, not a measurement of this account's billing. A cost that reads as measured when it was calculated is the same class of error as an unlabelled trend figure. | Yes |
| B-274 | 2026-09-19 | A proof example must point at a row in this database or it is not proof. | The standing rule against manufactured reviews survives on good intentions right up until a quiet week. A tester example points at a physical test, a customer example at a sale; a testimonial somebody typed is refused at the door. | Yes |
| B-275 | 2026-09-19 | The support service level is the minimum of its canonical and escalated figures, never an average. | One number over both lets a fast automated majority bury the slow escalated minority, and the escalated minority is where the unhappy buyers are. An untimed case is counted as untimed rather than dropped, because dropping it measures the cases somebody remembered to time. | Yes |
| B-276 | 2026-09-19 | Support response times are recorded once and cannot be revised. | A response time that can be revised is one somebody will revise, and the revision always goes the same way. | Yes |
| B-277 | 2026-09-19 | Timings are stored in an existing JSON column rather than a new one. | The deployed database is created with create_all, which adds tables and not columns. A new column would exist in every test and in no production row: a measurement that works everywhere except where it matters. | Yes |
| B-278 | 2026-09-19 | A funnel stage that killed nothing is refused, not warned about. | A kill gate with no kills either had nothing to judge or was not applied, and both mean the stage did not happen. A warned-about funnel is a funnel that ran. | Yes |
| B-279 | 2026-09-19 | An external observation can never be promoted into internal causal evidence. | The step is a single sentence and it is never noticed: somebody reads that textured stitches are trending, it goes in a brief as context, the next brief cites the first, and six weeks later it is something the company knows with nobody able to say who measured it. as_evidence() exists precisely so it can refuse by name. | Yes |
| B-280 | 2026-09-19 | A storefront takeover declares its revert date when it is planned. | The failure is not the Christmas banner going up late; it is the Christmas banner still being up in February, which says nobody is home more loudly than an empty shop. The end comes from the same calendar as the start, so it exists before anybody is busy. | Yes |
| B-281 | 2026-09-19 | Six brand elements may never change in a seasonal takeover. | A shop that resets its wordmark, palette anchor, photographic language and voice every quarter is not seasonal, it is unrecognisable, and the timeliness it buys is worth less than the recognition it spends. Policy text is on the list because those are promises rather than decoration. | Yes |


## Standing constraints carried from the spec

These are not re-decidable by a future session without the owner:

- Patterns are software releases. Never generate a beauty image and ask a model to guess the
  instructions (section 2).
- No fake reviews, buyers, favourites, sock-puppets, deceptive discounts or artificial
  engagement (Execution Directive, section 8).
- Never bypass CAPTCHA, KYC, identity checks or legal acceptance.
- Never copy competitor instructions, charts, photography or protected designs. Research is
  for demand and merchandising intelligence only.
- Never claim an integration, deployment, pattern, listing, test, campaign, customer or
  revenue exists until verified.
- Deterministic pattern validation wins even if every LLM disagrees (section 27).
