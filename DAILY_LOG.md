# DAILY_LOG

Meaningful work, results, and next actions. Newest first.

## Day 2 — 2026-09-16

- Owner shared a competing AODA-compliance pitch ("Ontario Compliance Engine") and said they think it beats the current strategy. Evaluated it on its merits rather than the pitch copy: fetched the live URL and found an unbuilt floot.app landing page (one working control, no functioning assessment/evidence/document features, CA$0 revenue and 0 customers per its own numbers). The two-tier design (website/WCAG check scoped to 50+ employees only) is a genuine fix to the exact flaw that killed the original AODA scanner (D-005: the 20-49 tier has no website obligation) — folded into the deferred EXP-003 pre-test spec (D-014, `BACKLOG.md`). Not adopted as a Track A/B replacement: new-domain SEO still can't rank in 90 days, the build is bigger than either running track, and Track B's dropshipping picks already have real Ad Library evidence behind them that would be wasted.
- Owner asked two follow-up strategy questions (90 days vs. a hypothetical 1-year window; a profit estimate for a fresh idea at 365 days) — answered with reasoned ranges in chat, not written to the ledger or projections file since the competition is still 90 days and no rule change is confirmed. Offered to write it up properly in `finance/PROJECTIONS.md` if the owner actually extends the challenge.
- heartbeat 2026-09-16T16:14Z: lock acquired; still no owner-block credentials in environment. Rebuilt listings 1-4 from scratch in this fresh session (`.venv` and `dist/` persisted from the prior heartbeat but are gitignored, so recreating them costs nothing — reran `build_templates.py`/`package.py`, `build_listing2.py`/`package2.py`, and listings 3-4's scripts) and re-ran all three formula-validation tests: 0 errors across all of them, no regressions. **Built and packaged listing 5, the bundle** (`package5.py`): zips the four existing workbooks plus one combined README into a single 5-file, 0.08 MB ZIP (well within Etsy's 20 MB / 5-file cap) — no new tax logic, so no new formula test needed. Caught and fixed an accuracy bug before committing: the listing copy claimed the bundle includes a Quick-Start PDF, but the packaging script only ships the four workbooks + README (no PDF) to stay within the file cap; corrected `listings.json`'s description and materials field to match what's actually in the ZIP. Title 130/140 chars, 13 tags ≤20 chars — within Etsy limits. **All five planned Track A listings (1-4 + bundle) are now built, validated and packaged; nothing is left to build before publishing.** Next: still blocked on owner Block 1 (Etsy shop + API keys); unblocked work remaining is the ledger importer, `ops/status.py` Etsy extension, and the Printify/POD line.
- heartbeat 2026-09-17T00:14Z: lock acquired; still no owner-block credentials. Built the **Printify API v1 client** (`products/pod/printify_api.py`, D-008 print-on-demand line): shops, catalog/blueprints, print providers with a Canadian-provider filter (C19: ship from Canada in 4-8 days, no cross-border duties), variants, image upload (base64), product create/publish, orders — same retry/error-handling pattern as `etsy_api.py`. Confirmed the exact endpoint paths and request bodies against Printify's own API reference and a second source before writing it (the upload endpoint in particular: `POST /uploads/images.json` with `{file_name, contents}`, not the `/uploads.json` a first search result guessed). 10 offline fixture tests, 0 failures, no live calls made (waiting on `PRINTIFY_TOKEN`). While in there, found and fixed two stale docs: `products/README.md` still said "Day 1: no products yet" despite five built Etsy listings — rewrote it with real status and test/build commands for all three product directories (etsy-templates, store, pod); `CREDENTIALS_SETUP.md` said the Etsy API helper was "to be written" when it's been done and unit-tested since Day 1. Next: 3-5 Canadian-gift designs (mugs/prints/totes) rendered programmatically for the POD line; Track B SPEC modules 0-3 (safety rails, Shopify/CJ/Meta clients) can also be built offline with fixtures per the spec's own instructions.
- heartbeat 2026-09-17T08:14Z: lock acquired; still no owner-block credentials. Researched Printify's personalization capabilities before designing anything: confirmed they have a native Personalization Studio (buyer-entered text, automatic rendering and mockup generation via an async API) — meaning no custom per-order rendering pipeline is needed for the POD line, only configuring it once the shop is connected. Built **3 of the 5 target Canadian-gift designs** (`products/pod/build_designs.py`, Pillow-rendered): a "Welcome to \<Town\>" mug wrap, "The \<Family\> Cottage" art print, and a "Proud Hockey \<Role\> of \<Name\> #\<Number\>" tote. Caught and fixed two real bugs before calling them done: (1) a spacing bug where a decorative divider line cut through the mug's town-name text like a strikethrough; (2) a more serious one found only by deliberately stress-testing with realistic long inputs ("Sault Ste. Marie", "Papineau-Beauchamp", "grandmother") rather than the short happy-path samples — fixed-size text ran off both edges of the canvas, which would have silently produced broken print files for any buyer with a longer name. Fixed with a `fit_font()` auto-shrink helper applied to every personalizable string, and added `test_build_designs.py` (7 tests, incl. long/short stress cases) so it can't silently regress. Logged the lesson as LL-010. Wrote `products/pod/DESIGN_SPEC.md` documenting the personalization architecture, the pixel-dimension caveats (placeholders until a real Printify blueprint is queried), and what's still open. Updated `products/README.md` to match. Next: 2 more designs; once `PRINTIFY_TOKEN` exists, confirm exact blueprint dimensions and configure the Personalization Studio text layer.

## Day 1 — 2026-09-15

- Received the Master Operating Directive; extracted to `docs/MASTER_DIRECTIVE.txt`.
- Recorded competition start 2026-09-15T11:00:07Z in `COMPETITION_RULES.md`.
- Created the persistent project operating system (this repository's state files, ledger, dashboard, logs).
- Verified capabilities available to Claude in this environment: web search/fetch, GitHub API (as `StateFarm91`), scheduled Routines (fresh sessions on a cron), Node 22 / Python 3.11 toolchains. No payment, hosting, domain, or marketing accounts exist yet.
- Infrastructure research completed and saved: `docs/infra/HOSTING_RESEARCH.md` (Cloudflare Workers recommended; GitHub Pages bars business use; sandbox cannot enable Pages or write repo secrets) and `docs/infra/PAYMENTS_RESEARCH.md` (Stripe Managed Payments recommended; Polar.sh fallback).
- 11:26Z: the first opportunity-sweep workflow hit the owner's five-hour usage limit after 3 of 12 lens researchers finished (LL-001). Their 15 candidates were recovered from the workflow journal into `research/candidates/raw/`.
- 16:27Z: usage window reset; relaunched the nine remaining lenses as three lean researchers (D-004).
- Lean sweep completed (27 more candidates; 42 total) → `research/candidates/CANDIDATES.md`; 16 verification searches; ranking of all 42 (`research/OPPORTUNITY_RANKING.md`); five finalist deep-dives (`research/finalists/`).
- Adversarial review (`research/ADVERSARIAL_REVIEW.md`) killed the AODA scanner's demand thesis and the Google Ads test; adopted the sequential Etsy-first variant.
- **Strategy selected (D-005): MapleSheets, Etsy shop of Canadian small-business templates.** `MASTER_STRATEGY.md`, `EXPERIMENTS.md` (EXP-001/002/003), `finance/PROJECTIONS.md`, `OWNER_ACTIONS.md` Block 1 written. Working names MapleLedger/NorthLedger rejected (existing Canadian firms).
- Built listing 1: `products/etsy-templates/build_templates.py` (9-sheet T2125 workbook), validated with the `formulas` engine (0 errors / 6,122 cells; key values checked), packaged (ZIP + QuickStart.pdf + README + 6 images) by `package.py`; copy in `listings.json` within Etsy limits. LibreOffice here lacks Calc, so validation uses the Python evaluator.
- Etsy API client `etsy_api.py` (PKCE auth, search, taxonomy, create/upload/activate, receipts) with 8 offline tests passing.
- Heartbeat design finalized (`ops/HEARTBEAT_PROMPT.md`, `ops/lock.py`).
- Listing 2 built and validated (`build_listing2.py`), copy and images packaged; shop policy/About text written (`SHOP_TEXT.md`).
- Heartbeat: first design (fresh session per firing) failed its test (no repo attached; LL-006); replaced by persistent operator session `session_01GpoRAgr4kBWxi1752HufQh` + Routine `trig_019rtbCKLSFc8hNajuiWm9E4`; plumbing check passed (the operator session pulled, committed and pushed).
- Founding session ends ~17:20Z. Spend to date: $0.00. Revenue: $0.00. Owner-only blocker: Block 1 in OWNER_ACTIONS.md.
- Evening: owner challenged the Etsy-only plan and removed all setup restrictions; Claude re-decided on its own evidence (D-009/D-010): Track A floor + capped Track B paid-social store. Owner Block 2 written (Shopify, CJ, Cloudflare, Anthropic, Meta, fal.ai). Track B spec (`products/store/SPEC.md`), money caps (`ops/caps.py`, tested) and product research (D-011) completed. ffmpeg available via imageio-ffmpeg.
- Next: owner Blocks 1 and 2 → Etsy OAuth + listings + EXP-001; store stack build per SPEC; CJ verification of products; launch checklist → EXP-004. Unblocked work for heartbeats: SPEC modules 0-3 with offline tests, Etsy listings 3-4, ledger importer.
- heartbeat 2026-09-15T17:14:08Z: operator session plumbing check; lease held by session_01HgvZLG32EjL8kfcsoWVTmJ, no work
- heartbeat 2026-09-16T00:14Z: lock acquired (session_01GpoRAgr4kBWxi1752HufQh); no Etsy/Shopify/other Track A-B credentials present in environment, both owner blocks still open. Built and validated listing 3, **Home-Office and Vehicle Expense Workbook** (`build_listing3.py`, standalone Home office + Vehicle sheets adapted from listing 1, printable logbook page added; net-income cap is now a direct input since there is no ledger to derive it from). `test_listing3.py`: 0 formula errors across 120 cells; key values checked including an ad-hoc non-default run confirming the 7N loss cap (7P correctly limited to $500 net income against an $875 potential claim). Packaged copy + 3 images (`package3.py`) and added the `listing-3` entry to `listings.json` (title 125/140 chars, 13 tags ≤20 chars each — within Etsy limits). Created `.venv` from `.venv-requirements.txt` (did not exist yet this session). Next build item: listing 4 (instalment planner) needs 2026 federal/provincial tax brackets and CPP rates verified by web search before building — not done this session.
- heartbeat 2026-09-16T08:14Z: lock acquired; still no owner-block credentials in environment. Verified 2026 federal tax brackets directly against CRA's own rates page, all 13 provincial/territorial brackets and 2026 CPP/CPP2 self-employed rates cross-checked against independent sources, and the Quebec 16.5% federal abatement rule — written up in `TAX_REFERENCE.md`. Built and validated **listing 4, Self-Employed Tax Set-Aside and Instalment Planner** (`build_listing4.py`): federal tax with BPA credit and phase-out, all-province tax via a 2D bracket lookup table, CPP base + CPP2, monthly set-aside, CRA instalment rule of thumb with due dates. Deliberately conservative (excludes provincial credits and the CPP self-employment deduction) and discloses this on the sheet, so the estimate is a safe overestimate, never low. `test_listing4.py` (Ontario $50k default): 0 formula errors across 472 cells, all 15 checks pass. **Caught a real bug during ad-hoc testing**: the `formulas` Python validation engine silently mis-evaluates `INDEX(range,0,col)` (the "whole column" trick, valid in real Excel/Google Sheets) — it returned a wrong single value with no error flag, which a hand-computed Quebec/$90k check caught. Redesigned to stage the selected province's 8 brackets into hidden columns via plain single-cell INDEX calls, then re-verified three scenarios (Ontario $50k, Quebec $90k incl. abatement + CPP2, Alberta $300k incl. BPA floor) all matching hand calculations exactly. Packaged copy + 3 images (`package4.py`), added `listing-4` to `listings.json` (title 122/140 chars, within Etsy limits). **All four planned Track A listings are now built and validated.** Next: listing 5 (bundle, CA$44) needs only packaging, no new tax logic.
